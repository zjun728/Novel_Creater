# 真实流程长篇生成 QA - 2026-06-11

## 测试范围

- 测试项目：`QualityBalanceQA200w_20260610Run20 _20260610075312`
- 项目 ID：`32e1751a-0de3-424e-bf1d-51c4ed385236`
- 本地入口：`http://127.0.0.1:5173/project/32e1751a-0de3-424e-bf1d-51c4ed385236`
- 使用模型：`联通云-DeepSeek-V4-Flash / DeepSeek-V4-Flash`
- 测试目标：按真实用户流程跑通 20 章生成、审稿、修订、定稿、记忆提取、设定变更处理、多章一致性验收和浏览器冒烟验收。
- 详细报告：
  - `tmp/realistic-flow-qa/latest-realistic-report.json`
  - `tmp/realistic-flow-qa/latest-realistic-report.md`

## 本轮修复

- 记忆提取增加本地兜底：当模型没有返回可解析 Canon 事实时，基于章节正文和摘要生成最小可用事实，避免后续章节完全没有记忆可用。
- 定稿事实默认进入可用状态：`processChapterFinalization` 保存 Canon 事实时保留原状态，缺省按 `accepted` 入库，避免事实长期停留在待确认状态导致上下文不可用。
- 正文字数验收改为质量优先：QA 阻断阈值放宽到目标字数的约 `70%-150%`，避免为了精确字数牺牲章节完整性；仍记录超出建议范围的章节。
- 空正文保护：正文生成、节奏修订、压缩、纠偏草案如果返回空内容，会直接阻断保存，避免空候选或空正文污染版本列表。
- 压缩过度恢复：当正文压缩后过短时，使用原稿重新平衡压缩；仍过短时尝试补足，优先保留情节因果和场景反应。

## 验收结果

- 检查项总数 63，通过 59，非通过 4。
- 20 章均完成定稿。
- 20 章均存在可用 Canon 事实。
- 待确认设定变更为 0，未阻断后续章节生成。
- 多章一致性验收通过，未发现硬性设定冲突。
- 项目级审稿报告已保存，报告中记录 5 个项目级问题。
- 浏览器冒烟验收通过：项目详情页、选题、种子、圣经、设定库、章节管理、纠偏任务、写字台、审稿入口均可打开。
- 浏览器控制台错误数：0。

## 本轮暴露的问题

- 第 17 章第 1 次压缩返回空内容，当前已丢弃空候选，未写入版本列表。
- 第 17、19 章审稿局部修订未应用，说明安全补丁生成或片段定位仍不稳定。
- 第 20 章小纲压缩未降低长度，仍停留在 1819 字符。
- 正文源头仍容易超写：模型经常先生成 9000-12000 字，再依赖压缩链路收回到目标区间，源头小纲和正文提示词仍需继续优化。
- 小纲压缩不稳定：个别小纲压缩后仍超出建议上限，且短小纲有时仍会进入后续生成流程。
- 节奏修订的采用标准不够硬：个别章节修订后短段比例或连续短段反而更高，应改为“只有指标改善或不变才采用”。
- 审稿局部修订仍有应用失败：第 17、19 章出现局部修订未应用，说明片段定位和替换策略还不够稳。
- 章名生成仍不稳定：部分章节生成的章名不合格，系统保留默认章名。
- 结构化 JSON 输出仍频繁触发兜底：说明模型返回稳定性不足，后续需要继续加强解析、重试和更小粒度的结构化调用。

## 下一步优先级

1. 修复小纲质量闸：小纲过短、过长、压缩无效时必须重试或阻断，不能继续进入正文生成。
2. 修复节奏修订采用策略：只采用质量指标改善或至少不恶化的修订稿。
3. 修复审稿局部修订定位：改为更稳定的上下文滑窗定位，失败时保留可见原因和人工替换建议。
4. 优化章名生成：从全文抽象命名，不允许截取正文短句作为标题。
5. 继续源头控字数和反 AI 腔：减少后期压缩依赖，保持章节完整度和真人写作质感。
