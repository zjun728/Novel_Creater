# 开发日志

> 当前轻量开发日志。旧长日志、旧测试报告和临时归档已清理；本文件只记录当前仍有效的产品级决策。

## 2026-06-17 文档事实来源清理

### 清理原因

根目录规划和日志文件已经过大，新线程或上下文压缩后重新扫描项目时，很容易把旧规则、旧测试结论和临时补丁当成当前产品方向。

已清理完整历史文档和旧归档路径，避免后续线程把过期补丁当成当前规则。

已创建新的当前事实来源：

- `STORY_QUALITY_CHARTER.md`
- `CURRENT_PROJECT_STATE.md`
- 轻量版 `PRODUCT_DEVELOPMENT_PLAN.md`
- 轻量版 `FUNCTION_TEST_CHECKLIST.md`

### 产品决策

平台必须优先生成可读、好看、能持续推进的长篇小说，而不是追求文学实验、复杂隐喻或指标合规。

2026-06-17 讨论的微信文章带来两个关键提醒：

- 读者反感高阅读负担。正文不应让读者频繁做阅读理解。
- 读者会因为明显 AI 痕迹产生信任感崩塌，开始怀疑文字背后是否有具体的人、真实观察和判断。

这些内容已经记录到 `STORY_QUALITY_CHARTER.md`。

### 当前方向

- 正文生成保持轻量，并以讲故事为中心。
- AI 风格指标主要作为审稿和 QA 观察，除非严重破坏结构和可读性。
- 定稿前必须阻断结构性坏稿。
- 开发线程不能把局部测试失败直接升级成全局写作规则。

## 2026-06-17 旧真实流程测试产物清理

### 清理内容

旧真实流程报告、截图、本地日志、公众号文章抓取文件和人工章节审查文件已清理。

当前 `tmp/realistic-flow-qa/` 只保留故事块链路相关的最新 dry-run 报告和说明，后续真实流程测试必须重新生成干净报告。

### 产品决策

旧测试项目和旧报告只作为反面样本或历史证据，不再作为新链路验收标准。

新一轮重构不考虑旧项目、旧章节和旧 QA 字段兼容，可以按新的故事块驱动平台重新设计。

## 2026-06-17 故事块滚动规划准则

### 产品决策

新增故事块作为分卷规划和章节小纲之间的正式规划层。

故事块控制一段连续剧情的方向、功能和容量；章节小纲控制当前可写的小阶段。故事块不预设固定章节数，章节数量由实际故事阶段自然决定。

每章定稿后应执行块级回看，判断当前故事块是否继续、调整、结束或切换到新块。

正文超量时，优先拆章、更新剩余阶段或开启新故事块，不机械按长度切分长正文。章节结尾不强求强钩子，只要求自然停顿点。

### 2026-06-17 补充边界

故事块滚动规划只能向前滚动，不能回改已执行内容。已有定稿章节的故事块，不得回改 `goal`、`entry_state`、已完成阶段和已执行故事任务。

v1 不提供旧的回改当前块动作。如果需要微调故事块，只能使用 `adjust_remaining_stages` 调整尚未执行、未被小纲引用、未被定稿章节依赖的剩余阶段。如果当前块后续方向变化较大，应提前结束当前块并开启新故事块承接，而不是重写旧块。

## 后续日志写法

本文件只记录当前有效的决策级内容。

不要把完整测试日志粘贴进本文件。详细运行产物放到 `tmp/realistic-flow-qa/` 或专门的短期产物目录，必要时由主产品线程决定是否保留；本文件只总结：

- 改了什么。
- 为什么改。
- 验证了什么。
- 还剩什么问题。
