# 小说创作平台 - 本地版产品规划 v5

## 1. 产品定位

构建一个面向个人创作者的本地版 AI 长篇小说创作工作台。

当前版本不做云端账号、团队协作、在线发布、付费体系，优先解决一个核心问题：

> 让 AI 可以全程参与长篇小说创作，同时保留想象力、发散性和意外生长，并通过本地记忆系统避免长篇写作失控。

这个产品不是传统意义上的“大纲管理器”，也不是简单的“AI 续写器”。它更像一个本地创作引擎：

- AI 负责构思、发散、正文生成、局部改写、审稿、总结、状态提取。
- 用户负责方向选择、审美判断、定稿确认、取舍和最终控制。
- 系统负责记忆、版本、上下文组织、模型调度、设定一致性和创作安全网。

## 2. 核心判断

全程 AI 创作是可行的，但不能采用“先规划完整 500 章，然后按表施工”的思路。

长篇小说的生命力来自不确定性。人物会在写作中长出来，伏笔会变形，支线会突然变重要，原本的结局也可能被更好的想法替代。因此产品应该遵循：

- 远景模糊，近景清晰。
- 先发散，再收束。
- AI 可以大胆提出可能性，但只有用户确认后的内容才能进入正式世界。
- 大纲、角色状态、伏笔管理退居后台，成为安全网，而不是创作的枷锁。
- 系统不能把小说写成任务清单。

## 3. 本地版边界

### 3.1 当前版本做什么

- 本地浏览器运行前端 + FastAPI 后端服务。
- 数据存储使用本地 MySQL 5.7，通过 FastAPI REST API 访问。
- 用户自行配置大模型 API Key（前端直连 AI 供应商，不经过后端）。
- 支持多个模型供应商配置。
- 支持本地项目、章节、草稿、定稿、版本、记忆库。
- 支持结构化设定库，记录人物、势力、世界观、修炼体系、物品和状态变更。
- 支持 TXT/Markdown/JSON 导出备份（后端提供全量导入导出 API）。
- 支持选题材与市场观察入口，但第一版以手动导入和公开元数据整理为主。

### 3.2 当前版本不做什么

- 不做云端同步。
- 不做用户账号。
- 不做在线发布。
- 不做 AI Gateway（AI 请求前端直连供应商，不经过后端代理）。
- 不抓取付费正文、登录后内容或受限制内容。
- 不把热门小说正文作为训练语料。
- 不承诺自动生成商业精品长篇。
- 不做重型爬虫平台。

## 4. 产品原则

### 4.1 创作优先

用户进入产品后看到的首先应该是创作现场，而不是复杂后台。

主界面应该围绕三件事：

- 当前正在写什么。
- AI 能给出哪些有想象力的可能性。
- 已经定稿的事实是什么。

### 4.2 保留未知

不要一开始强制生成完整全书大纲。更好的结构是：

- 未来 3-10 章：相对清晰。
- 当前卷：有方向和压力。
- 全书远景：只有主题、命运压力、核心矛盾和可能结局。

### 4.3 先野后稳

AI 创作分两层：

- 发散层：脑洞、反转、支线、人物秘密、场景、对白、设定变体。
- 正史层：用户确认后的章节、设定、人物状态、伏笔状态。

发散层可以大胆，正史层必须稳定。

### 4.4 不污染正式稿

所有 AI 生成内容默认进入候选区、草稿区或版本区。

只有用户点击确认后，内容才进入：

- 正式章节。
- 正式设定。
- 角色状态。
- 伏笔状态。
- 长期记忆库。

### 4.5 结构服务创作

三级大纲、章节摘要、角色状态、伏笔提醒都有用，但它们不应该成为作者的主要负担。

系统应该自动整理，用户只需要在关键节点确认。

## 5. 用户核心流程

### 5.1 选题与创作种子

用户可以从两个入口开始：

1. 自己输入一个想法。
2. 进入“选题雷达”，查看热门题材和市场趋势。

系统生成 3-8 个故事种子，每个种子包括：

- 题材类型。
- 暂定书名。
- 一句话故事。
- 核心卖点。
- 主角。
- 主角欲望。
- 核心矛盾。
- 世界压力。
- 开局钩子。
- 主要情绪价值。
- 潜在差异化。
- 风格目标。
- 风险提示。
- 结局锚点。

用户选择一个种子进入创作。

结局锚点不是强制结局大纲，而是故事最终可能抵达的画面、情绪收束或主题归宿。它用于给长篇创作提供方向感，不能用来锁死中途想象力。

选题雷达中的 AI 选题顾问支持两种种子动作：

- 生成新种子：根据市场数据、当前对话和用户偏好新增候选种子。
- 更新当前种子：根据当前选中的种子和最近对话，生成修订后的完整种子并更新当前种子，不新增候选。

AI 选题顾问的对话记录属于项目创作资产，应持久化保存，便于后续基于历史讨论继续调整种子。

### 5.2 发散设定

AI 基于种子生成多组可能性：

- 世界观方向。
- 主角版本。
- 反派版本。
- 关键秘密。
- 关系张力。
- 题材混搭方案。
- 可能的意外转向。
- 风格样章。

这些内容先进入“可能性池”，不直接成为正式设定。

### 5.3 建立创作圣经

用户确认后，系统形成最小创作圣经：

- 作品一句话。
- 题材与目标读者。
- 主角核心欲望。
- 核心冲突。
- 世界规则。
- 风格要求。
- 禁忌与不想写的方向。
- 已确认人物。
- 已确认设定。

### 5.4 滚动大纲

系统不强制全书完整大纲，而是维护四层结构：

- 当前章小纲：只服务这一章正文生成，由写字台章节流程单独生成、审阅、保存。
- 近景滚动规划：未来 3-5 章的章节目标、承接点、情绪节拍和暂不解决的问题；进入章节生成上下文，用来防止上下章断层。
- 当前卷规划：本卷 10-60 章的大结构，包括阶段目标、核心冲突、人物弧光、主要势力和卷尾状态；和分卷规划互相校准。
- 远景粗纲 / 长线蓝图：后面几卷的大方向、最终压力、可能收束和暂不解答的大问题；不细化到章节，也不直接进入每章正文生成。

每写完一章，AI 可以提出：

- 继续原路线。
- 人物失控路线。
- 更商业爽点路线。
- 更黑暗路线。
- 更文学化路线。
- 意外反转路线。

用户选择后，滚动更新近景规划。

长线蓝图属于低频滚动资产：每卷完成、分卷阶段总结或全局审稿后，可以结合创作圣经、创作种子、设定库、当前卷实际正文和分卷总结重新校准后续卷方向。它用于保持长篇方向感，不用于把后续剧情提前写死。

滚动规划必须遵守进度锁：

- `currentChapterNum` 表示当前待写章节，不是已写章节。
- 近景滚动规划只能从当前待写章节开始递增，不能回退到已写章节之前。
- 已定稿正文、设定库和已确认事实优先级高于旧规划；如果旧规划与已写正文冲突，应滚动修正规划，而不是重写已发生剧情。
- 已发生过的“首次”事件不能重新规划，例如首次获得系统、首次加点、首次突破、首次进入宗门、首次发现核心秘密等，只能承接后果。

近景滚动规划必须支持定稿后重路由：

- 每章定稿、摘要与记忆/设定提取完成后，系统应结合本章实际正文、章节摘要、已提取事实、待确认设定变更、当前卷规划和原近景规划，自动校验未来 3-5 章是否仍然成立。
- 如果本章实际选择、人物状态、地点、时间或信息揭示方式让后续近景规划前提失效，应微调未来 3-5 章，而不是强行沿用旧规划。
- 重路由只修正未写章节，不回改已定稿正文；失败时不阻断定稿，但应提示用户可在章节管理页手动重新生成或调整规划。
- 远景粗纲仍保持低颗粒度，只在分卷完成、阶段总结或全局审稿后低频滚动，不参与每章自动重写。

长篇线索链必须进入记忆检索：

- 定稿后提取 Canon 事实时，除普通摘要外，还应为事实打上可检索的线索标签，例如 `#主线推进`、`#主角身世线`、`#女主秘密线`、`#反派阴谋线`、`#关键道具线`、`#功法代价线`、`#势力斗争线`、`#感情关系线`。
- 章节生成上下文不只读取最近事实，也应根据当前章小纲、近景规划、当前卷目标和纠偏任务，定向检索相关线索链中的历史事实。
- 线索链用于防止百万字后遗忘早期伏笔、身份秘密、道具来源和长期阴谋；它不应把全书事实粗暴塞进模型，而是按当前章相关性摘取。

状态账本必须区分硬设定和动态变量：

- 设定库保存相对稳定的硬设定，例如人物身份、势力归属、世界规则、功法体系和物品基础属性。
- 状态账本/Canon 事实保存动态变量，例如伤势、位置、当前时间、物品持有者、剩余次数、冷却时间、已知/未知信息、视角可见范围、分线发生地点。
- 每章生成前应注入当前章相关的时间、地点、视角可知范围和人物/物品状态，防止同一角色在不同地点同时出现、已失去物品又重复使用、已受伤后下一章无故恢复等问题。

对于 100 万字以上长篇，还需要补一层“分卷 / 阶段管理”：

- 每一卷有明确章节范围、目标字数、阶段目标、核心冲突、关键人物和阶段摘要。
- 分卷是长篇创作的中间锚点，位于创作圣经和单章之间。
- 分卷完成后可以运行“分卷审稿”，检查这一卷的主线推进、人物弧光、设定一致性、伏笔状态和节奏问题。
- 分卷审稿结果应保存为项目资产，供下一卷写作继续参考，而不是一次性聊天结果。
- 分卷推进到阶段节点后可以生成“分卷阶段总结”，把已发生剧情、人物状态、设定变化、伏笔状态和下一卷接力点压缩成可复用记忆。

### 5.5 章节生成

每章可以生成多个候选版本，而不是直接覆盖正文。

章节生成前必须先形成“本章小纲”：

- 如果点击生成本章时没有小纲，系统先生成小纲，并弹窗让用户审阅。
- 用户可以在弹窗内手动修改、重排、补充小纲。
- 小纲属于章节级资产，生成、重新生成或手动保存后必须持久化。
- 刷新页面或切回章节时应恢复已确认小纲。
- 生成正文和生成多候选版本都必须基于当前保存/确认的小纲，不绕过小纲直接生成。
- 单章建议字数由项目目标字数 / 目标章节数推导；例如 300 万字 / 600 章时，正文生成目标约 5000 字。
- 字数约束用于控制章节体量和节奏，不要求机械精确；默认建议范围为目标字数约 90%-130%，硬边界为约 80%-140%。例如 300 万字 / 600 章时，目标约 5000 字，建议 4500-6500 字，硬边界 4000-7000 字。
- 字数护栏不能牺牲章节质量：不得为了压字数省略关键动作、人物反应、情绪转折、因果交代或章节钩子；内容超量时优先判断是否塞入两章容量，能拆则在自然断点拆到下一章。
- 小纲生成阶段也应读取本章建议字数，按单章体量设计节拍，避免把两章体量规划进一章；默认小纲 4-6 个节拍、700-1100 字，只锁定关键场景和人物选择，不把对白与动作规定死。

推荐默认候选：

- 稳妥推进版。
- 强冲突版。
- 意外转向版。
- 文学气质版。
- 纠偏候选版：由审稿纠偏任务生成，只用于修正指定问题，不直接覆盖正文。

用户可以：

- 选择一个版本作为草稿。
- 融合多个版本。
- 局部重写。
- 让 AI 继续扩写。
- 手动编辑。
- 将候选版本加入对比池，尤其是纠偏候选与当前定稿/草稿对比后再决定是否采纳。
- 对比池支持选择两个版本做段落级差异对比，先展示字数变化、新增段落、删除段落、保留段落和改动预览，不自动覆盖正文。

### 5.6 定稿入库

用户点击“确认定稿”后：

- 正文进入正式章节表。
- 自动生成章节摘要。
- 自动提取角色状态变化。
- 自动提取新增事实。
- 自动识别新增伏笔和回收伏笔。
- 生成“待确认变更”。

用户可以一次性确认、逐条确认或全部忽略。

只有确认后的内容进入长期记忆库。

章节定稿后的锁定规则：

- 章节一旦定稿，当前章正文、小纲、版本增删改和普通 AI 写作入口全部锁定。
- 定稿后禁止再次生成本章、重新生成小纲、基于小纲生成多候选、续写、扩写、压缩、选区改写、润色、另存普通草稿或删除版本。
- 定稿后仍允许查看正文、小纲、审稿、导出、查看上下文和记忆。
- 当前版本不提供“修改已定稿章节”能力，避免重新牵动记忆、设定库、上下章衔接和纠偏链路。
- 定稿必须走专用定稿接口，一次性完成版本 final 标记、章节 finalVersionId、章节状态和字数写入；不能先把版本标记为 final 后再调用普通章节更新接口，否则会被章节锁定保护拦截。
- 定稿执行必须具备幂等防重保护：版本列表定稿、轻微问题继续定稿、严重/主要问题后的“仍然定稿”都必须进入同一条执行链路；一旦同一项目、同一章节、同一版本进入定稿或定稿后处理，后续重复点击只能提示处理中，不能再次触发摘要、记忆或设定变更提取。
- 定稿后的摘要、记忆事实和设定变更提取完成前，写字台进入不可关闭的处理遮罩，禁止切换章节和执行任何会推进创作的 AI 操作。
- 下一章生成小纲、正文、多候选或局部改写前，必须确认上一章定稿后的入库处理已经完成；若上一章仍在提取中，应阻止生成，避免下一章缺失最新设定。
- 定稿后处理还应触发近景滚动规划重路由：在不修改已定稿正文的前提下，把本章真实结果同步到未来 3-5 章规划，降低上下章断层和旧规划失效风险。

设定库章节自动提取规则：

- 章节定稿后，系统会从正文中提取人物、势力、地点、体系、功法、物品和关系变化，生成待确认设定变更。
- 提取时必须同时参考已有设定实体和已有关系，优先更新已有记录，避免重复创建同名人物、同名势力或重复关系。
- 系统保存待确认变更前需要做基础去重，同一章节、同一实体、同一字段、同一新值不重复写入候选。
- 待确认设定变更支持逐条编辑、逐条确认、逐条拒绝，也支持批量确认和批量拒绝。
- 批量确认只适合用户已经审阅过的一组低风险变更；高风险主线、境界、功法和关系变化仍建议逐条检查。
- 待确认变更在确认前应做设定冲突检测：人物归属、身份、境界、功法、武器、位置、势力控制、体系规则、物品持有者等硬设定发生覆盖时，需要在界面显著提示。
- 有冲突风险的候选允许用户强制确认，但必须经过二次确认；批量确认默认跳过冲突风险项，要求用户逐条审阅。

## 6. 功能模块

## 6.0 创作流程状态

项目页顶部流程状态必须由真实数据驱动，而不是静态标签。

状态规则：

- 选题：项目已有题材或方向数据后显示“已开始”。
- 创作种子：无种子为“待完善”；有候选无选中为“待确认”；有选中为“已就绪”。
- 创作圣经：有核心内容后显示“已就绪”。
- 设定库：有待确认变更时显示“有待确认”；有正式设定时显示“已就绪”。
- 章节：已有正文或定稿后显示“已开始”。
- 纠偏任务：有未完成任务显示“待处理”；已处理过显示“已处理”；无审稿任务显示“待审稿”。

阶段保护原则：

- 创作开始前，种子、圣经、设定库可以自由重置。
- 创作开始后，核心资产只能局部维护，不能整块清空或删除。
- 已有正文后禁止清空种子、删除圣经、清空设定库。
- 所有会影响后续章节一致性的操作，必须有明确提醒或确认。

纠偏任务处理原则：

- 纠偏分为硬纠偏和软纠偏。
- 硬纠偏只用于未定稿章节；本章审稿发现的问题优先作为即时修稿输入，生成局部修订候选版本和差异对比，不强制进入纠偏任务板。
- 已定稿章节不再生成正文修订型硬纠偏任务；如果发现偏差，只能通过分卷/全局软纠偏在后续章节自然补解释、补动机或回收伏笔。
- 分卷纠偏默认是阶段结构诊断：不回改已定稿正文，输出后续章节修正任务、设定/记忆候选任务或卷结构调整建议。
- 全局纠偏默认是全书方向诊断：不回改已定稿正文，输出长期软修复、主线调整建议、伏笔回收任务或设定/记忆候选。
- 阻断型纠偏未处理时，生成小纲、正文、多候选、续写、扩写、压缩、选区改写、对比和定稿都应被拦截。
- 软纠偏未处理时，不阻断生成，但必须进入写作台 AI 上下文，作为未来章节的自然修复约束。
- 审稿问题转纠偏任务必须降噪：严重/主要问题优先生成纠偏任务；轻微风格建议默认留在审稿报告中，不批量进入纠偏任务板。
- 同类纠偏任务必须去重并限量：本章审稿最多转 3 条，分卷审稿最多转 5 条，全局审稿关键问题最多转 8 条，nextActions 最多转 3 条，避免纠偏任务过多反向干扰正文生成。
- 写字台 AI 上下文只注入高优先级纠偏：优先当前章、阻断型、严重/主要问题；低优先级任务保留在任务板，不全文注入模型。
- Canon 和设定类任务可以生成待确认候选，由用户确认后入库；生成候选不等于完成任务，必须在设定库/记忆中确认或拒绝后，再回到纠偏任务板点击“完成”或“忽略本次”。
- 伏笔、分卷规划、创作圣经和章节正文类任务第一版不自动修改内容。
- 这类任务先提供“定位处理”，跳转到对应模块或章节，让用户人工判断后处理。
- 未定稿章节的本章审稿可以生成“局部修订候选版本”，但只进入版本列表，不覆盖当前正文，不自动定稿；生成后应自动打开当前正文与修订候选的差异对比。
- 本章审稿局部修订优先使用“局部替换补丁”，只替换能安全定位的问题片段；如果模型未返回可用补丁或补丁无法命中正文，系统应重试一次，仍失败时提示用户使用审稿面板的“定位原文/替换”或手动选区改写，不自动整章重写兜底。
- 本章审稿报告关闭后，未定稿章节应在写字台保留“审稿修改建议”面板：展示问题、原文片段、修改建议和可直接替换文本，支持定位原文、逐条替换或忽略。
- 写字台逐条替换优先替换当前正文中仍能精确找到的 `location` 原文；找不到时可以进行安全容错定位，仍失败时必须提示重新审稿或手动修改，不允许语义猜测替换。
- 逐条替换定位允许忽略空白、换行、中英文引号和常见标点差异，但只有命中唯一位置时才允许替换；命中多个相似片段时必须提示人工处理。
- 如果审稿 `location` 只命中半句，而 `replacement` 是完整句/完整段，系统应自动扩展到当前完整句再替换，避免把完整句插入到半句话中。
- 审稿提示词必须要求 `location` 从正文逐字复制，并要求 `replacement` 与 `location` 粒度一致：半句替半句，整句替整句，整段替整段。
- 纠偏任务板保留给分卷/全局/跨章节/设定类问题；同章多条正文类硬纠偏任务可以合并生成一个综合章节修订候选版本。
- 点击定位后，任务可进入“处理中”，表示已经转入对应模块排查。
- 由纠偏任务生成的章节修订候选版本需要保留来源任务关联；当该候选被用户确认为定稿后，系统自动把关联纠偏任务标记为完成。
- 只有 `pending`、`accepted`、`in_progress` 状态的纠偏任务会进入写作台 AI 上下文。
- `done`、`rejected`、`ignored`、`cancelled`、`archived` 都视为关闭状态，不再提醒 AI，也不再计入未完成纠偏任务。
- “忽略本次”必须保留历史记录，但从后续正文生成、续写、选区改写上下文中移除，避免已经放弃的问题反复干扰创作。

## 6.1 项目库

用于管理本地小说项目。

字段：

- 项目名称。
- 题材。
- 简介。
- 目标字数。
- 目标章节数。
- 当前章节。
- 创建时间。
- 更新时间。
- 默认模型配置。

功能：

- 新建项目。
- 编辑项目基础信息：项目名称、题材、简介、目标字数、目标章节数。
- 打开项目。
- 删除项目。
- 导出项目。
- 导入项目备份。

项目基础信息编辑规则：

- 项目名称、题材、简介属于展示与定位信息，可以在项目生命周期内编辑。
- 目标字数和目标章节数属于规划尺度，会影响章节规划、进度判断和后续生成上下文。
- 项目尚未产生章节或正文候选版本时，可以编辑目标字数和目标章节数。
- 项目已有章节或正文候选版本后，目标字数和目标章节数默认锁定，不允许在项目库直接修改；如后续确需大改，应通过阶段规划或分卷规划调整，而不是改动项目地基。
- 项目详情页顶部也应提供项目信息编辑入口，避免用户进入项目后必须返回项目库才能修改名称、题材或简介。
- 项目库、项目详情页、写字台、设置页的关键入口文案必须保持可读中文；出现历史编码乱码时，优先修复可见按钮、弹窗、提示和错误信息。

## 6.2 多模型配置

多模型配置是底层核心能力。

小说创作不应该只绑定一个模型。不同模型适合不同任务：

- 正文创作。
- 脑洞发散。
- 大纲规划。
- 审稿检查。
- 摘要压缩。
- 结构化提取。
- 选题分析。
- 润色改写。

### 6.2.1 Provider 类型

第一版支持三类：

1. Claude 原生接口。
2. OpenAI-compatible 通用接口。
3. 自定义接口配置。

内置预设：

- OpenAI。
- Anthropic Claude。
- DeepSeek。
- 通义千问 / Qwen。
- Kimi / Moonshot。
- 腾讯混元。
- 百度千帆 / 文心。
- 自定义 OpenAI-compatible。

注意：具体模型 ID、base URL、鉴权方式可能随平台变化，开发时以各平台官方文档为准。产品层只保存可配置项，不把模型写死。

### 6.2.2 配置字段

```js
{
  id: 'uuid',
  name: 'Claude 主创',
  providerType: 'anthropic' | 'openai-compatible' | 'custom',
  baseURL: '',
  apiKey: '',
  model: '',
  stream: true,
  maxContextTokens: 200000,
  maxOutputTokens: 4096,
  temperature: 0.8,
  topP: 0.9,
  supportsJSON: true,
  supportsStreaming: true,
  notes: '',
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### 6.2.3 任务模型映射

```js
{
  projectId: 'uuid',
  writingModelId: 'uuid',
  brainstormModelId: 'uuid',
  outlineModelId: 'uuid',
  auditModelId: 'uuid',
  summaryModelId: 'uuid',
  extractionModelId: 'uuid',
  marketModelId: 'uuid',
  polishModelId: 'uuid'
}
```

### 6.2.4 模型试写对比

同一个章节目标可以让多个模型各写一版。

对比维度：

- 想象力。
- 情节推进。
- 文字风格。
- 人物声音。
- 商业爽点。
- 可继续性。

用户可以选择一个版本，或让 AI 融合多个版本。

## 6.3 选题雷达

选题雷达是“市场观察 + 创意孵化”，不是热门小说复制器。

### 6.3.1 目标

- 观察不同平台的热门题材。
- 按类型聚类。
- 给出每个题材下 10-20 本作品卡片。
- 提炼题材卖点、读者情绪、开局钩子和差异化机会。
- 帮助 AI 生成更贴近市场但不抄袭的原创故事种子。

### 6.3.2 数据来源

v0.4 实现方案：直接并抓取已知可读的排行榜页面，无需用户手动粘贴。

已知可读平台：

- 书旗小说（shuqi.com/rank）
- 纵横中文网（zongheng.com/rank.html）
- 潇湘书院（xxsy.net/rank）
- 52书库（52shuku.net/Top）
- 小说阅读网（m.readnovel.com/rank）

技术方案：httpx.AsyncClient + asyncio.gather 并发请求 → 正则提取书名/作者/分类/简介 → 写入 market_items 表。新增平台通过扩展 KNOWN_RANK_PAGES 列表即可。

搜索引擎中转方案（DuckDuckGo HTML 搜索）经实测不可用，返回 202 状态码，已被放弃。

### 6.3.3 采集边界

只采集公开可见页面的元数据：

- 书名、作者名、平台、分类、标签。
- 简介（meta description 或页面公开摘要）。
- 榜单位置、字数、连载状态、热度指标。
- 公开 URL、抓取时间。

不采集：

- 正文、付费章节。
- 登录后内容。
- 受平台 robots.txt 或条款禁止抓取的内容。
- 任何需要绕过限制才能获得的数据。

### 6.3.4 输出内容

每个题材输出：

- 热门程度。
- 代表作品 10-20 本。
- 共同卖点。
- 常见开局。
- 常见主角类型。
- 常见冲突结构。
- 读者情绪价值。
- 同质化风险。
- 可创新空间。
- 推荐原创切入角度。

### 6.3.5 作品卡片

```js
{
  id: 'uuid',
  platform: 'qidian' | 'fanqie' | 'qimao' | 'shuqi' | 'manual',
  title: '',
  author: '',
  category: '',
  tags: [],
  rankName: '',
  rankPosition: 1,
  intro: '',
  wordCount: 0,
  status: 'serializing' | 'completed' | 'unknown',
  heatText: '',
  url: '',
  capturedAt: timestamp,
  aiSummary: '',
  extractedHooks: [],
  extractedAppeals: [],
  plagiarismRiskNotes: ''
}
```

### 6.3.6 选题生成

基于选题雷达，AI 生成原创选题时必须遵守：

- 不复刻具体作品设定。
- 不借用具体角色、剧情、专有名词。
- 只抽象题材趋势、读者情绪和结构机会。
- 每个选题必须给出差异化理由。

AI 选题顾问要支持项目内连续孵化：

- 对话历史持久化，刷新页面后仍可回看。
- 用户可以基于热点作品和方向建议继续追问、收束、修正。
- “生成新种子”用于新增候选种子。
- “更新当前种子”用于把刚才讨论形成的调整应用到当前选中的种子。
- 生成或更新种子时，AI 必须输出完整种子 JSON；系统负责解析、必要时修复 JSON、落库并同步到创作种子页。
- 如果 AI 输出内容无法解析或保存，必须明确提示失败原因，不能只把 JSON 显示在聊天框里。
- 种子 JSON 必须保留 `endingAnchor` 字段；内容可以为空，但字段不能省略。

## 6.4 创作种子

创作种子是项目的原点。

字段：

- 暂定书名。
- 题材。
- 一句话故事。
- 主角。
- 主角欲望。
- 核心矛盾。
- 世界压力。
- 开局钩子。
- 情绪价值。
- 差异化。
- 风格目标。
- 风险提示。
- 结局锚点。

结局锚点保留但非必填。AI 生成时必须保留字段，能判断时写出终局画面、情绪收束或主题归宿；信息不足时可以为空字符串。它只提供远景方向，不限制中途剧情生长。

功能：

- AI 生成多个种子。
- 手动创建种子。
- 手动编辑种子。
- 从粘贴 JSON / JSON-like / 中文标签文本直接保存种子。
- 更新当前选中种子。
- 用户收藏种子。
- 种子对比。
- 从种子生成创作圣经。

状态规则：

- 没有种子：创作流程显示“待完善”。
- 有候选种子但没有选中种子：显示“待确认”。
- 有选中种子：显示“已就绪”。
- 创建第一条种子时自动设为当前选中种子。
- 后续新增种子只作为候选，不自动覆盖当前选中种子。
- 清空种子后状态回到“待完善”。

创作阶段保护：

- 项目尚未产生正文内容时，可以新增、编辑、选择、删除或清空种子。
- 项目已有正文内容后，禁止新增、导入、生成、修改、另存、选择、删除或清空创作种子。
- 写作开始后的大方向调整应通过创作圣经局部编辑、设定库变更、纠偏任务或新草案机制处理，不直接重写种子地基。

## 6.5 可能性池

可能性池用于保存 AI 的发散内容。

它不是正式设定，只是创作燃料。

类型：

- 人物秘密。
- 支线。
- 反转。
- 场景。
- 台词。
- 设定变体。
- 伏笔候选。
- 结局候选。
- 风格样段。

字段：

```js
{
  id: 'uuid',
  projectId: 'uuid',
  type: 'character_secret' | 'plot_twist' | 'scene' | 'dialogue' | 'setting' | 'ending' | 'style_sample',
  title: '',
  content: '',
  source: 'ai' | 'user' | 'market-radar',
  status: 'candidate' | 'accepted' | 'rejected' | 'archived',
  relatedChapter: null,
  relatedCharacters: [],
  createdAt: timestamp
}
```

## 6.6 创作圣经

创作圣经只保存确认后的核心信息。
它是“这本小说应该如何成立”的总纲，不是百科全书式资料库。

包括：

- 作品定位。
- 风格圣经。
- 世界规则。
- 主题与母题。
- 核心人物原则。
- 核心关系原则。
- 世界观底层规则。
- 禁忌设定。

旧方案中的世界观圣经保留，但需要增加“风格圣经”和“主题母题”。
详细的人物、势力、地点、功法、物品和关系变化进入“设定库”，创作圣经只引用其中最核心、最稳定的部分。

从种子生成创作圣经只能发生在项目尚无创作圣经、尚未进入设定库或正文写作阶段时。已有创作圣经后，种子页不能再次生成并覆盖圣经。

创作圣经到设定库是一次性初始化动作，只用于项目初始阶段建立最小设定底座。初始化后，设定来源应转为“章节定稿提取 + 设定库人工确认”，避免旧种子或新圣经覆盖已经写过的世界状态。

创作圣经提取到设定库时，如果 AI 返回 JSON 格式损坏或被截断，系统应使用圣经和当前选中种子生成保守版设定候选，保证初始化流程不中断；这些候选仍必须由用户确认后才进入正式设定库。

初始化提取与章节增量提取必须严格区分：

- 初始化提取：来源是创作圣经和当前选中种子；触发时机是正式写作前、尚未初始化设定库时；目标是提取核心人物、核心势力、世界规则、能力体系、关键地点、关键物品和长期关系。
- 初始化提取适合分类型处理：人物、势力/组织、世界规则/能力体系、地点/物品、长期关系可以分批提取，再由前端合并去重。
- 章节增量提取：来源是已定稿章节正文 + 已有设定库；触发时机是章节定稿后；目标是记录本章真实发生的新实体、状态变化、关系变化、物品归属变化和世界规则揭示。
- 章节增量提取不能重新扫描整套圣经，不能批量重写旧设定，不能用规划内容覆盖已经写过的世界状态。
- 两种提取最终都只生成“待确认设定变更”，必须由用户确认后才进入正式设定库。

写作开始后：

- 禁止删除创作圣经。
- 允许局部编辑创作圣经，但保存前必须提醒：修改只影响后续写作上下文，不自动改写既有章节。
- 如果需要大改主线或世界观，应通过新版圣经草案、差异对比、纠偏任务或人工迁移机制处理，不直接覆盖正式圣经。

## 6.7 角色系统

角色不能只像 RPG 存档。
角色系统升级为设定库中的“人物档案”，人物弧光只是人物档案的视图之一，不作为唯一主数据源。

角色状态分两层：

### 6.7.1 硬状态

- 位置。
- 身体状态。
- 持有物。
- 身份。
- 所属势力。
- 已知信息。
- lastUpdatedChapter。

### 6.7.2 软状态

- 当前欲望。
- 恐惧。
- 误解。
- 执念。
- 情绪状态。
- 与主角关系。
- 秘密。
- 人物弧光阶段。

AI 写作时，硬状态防止错误，软状态保证人物有生命力。

## 6.8 伏笔与线索系统

伏笔不做硬性截止日期。

状态：

- candidate：候选伏笔。
- planted：已埋设。
- developing：正在推进。
- transformed：已变形。
- resolved：已回收。
- abandoned：放弃。

字段：

- 内容。
- 首次出现章节。
- 关联角色。
- 关联设定。
- 可能回收窗口。
- 回收方式候选。
- 当前状态。
- AI 提醒。

提醒语气应该是“建议推进”，不是“本章必须回收”。

## 6.9 滚动大纲

不强制完整全书大纲。

结构：

```js
{
  projectId: 'uuid',
  farVision: {
    theme: '',
    finalPressure: '',
    possibleEndings: [],
    unresolvedBigQuestions: []
  },
  currentVolume: {
    title: '',
    goal: '',
    mainConflict: '',
    emotionalArc: '',
    expectedChapterRange: [1, 60]
  },
  nearChapters: [
    {
      chapterNum: 1,
      title: '',
      goal: '',
      conflict: '',
      turn: '',
      emotionalBeat: '',
      requiredFacts: [],
      optionalSurprises: []
    }
  ]
}
```

### 6.9.1 分卷规划与阶段审稿

长篇项目除了远景 / 中景 / 近景，还需要单独的分卷层。

分卷字段：

- 卷序号。
- 卷标题。
- 起始章节。
- 结束章节。
- 目标字数。
- 阶段目标。
- 核心冲突。
- 关键人物。
- 阶段摘要。
- 当前状态。
- 最近一次分卷阶段总结结果。
- 最近一次分卷审稿结果。

分卷审稿输出：

- 当前卷总体评价。
- 当前卷已完成了什么、还缺什么。
- 主要优点。
- 主要问题（按严重度区分）。
- 人物弧光判断。
- 设定一致性判断。
- 伏笔推进 / 回收状态。
- 节奏判断。
- 下一步行动建议。

分卷阶段总结输出：

- 阶段总览与短摘要。
- 已完成的关键剧情节点。
- 未解决问题和悬念。
- 人物状态 / 关系 / 目标变化。
- 设定、地点、势力或体系变化。
- 伏笔当前状态。
- 下一卷接力点。
- 后续不能写错的连续性约束。
- 下一卷可自然展开的剧情种子。

写作台上下文接入规则：

- 生成本章小纲、生成本章正文、多候选版本时，必须注入当前章节所属分卷的阶段目标、核心冲突、关键人物、阶段摘要、阶段总结、连续性约束和最近审稿结论。
- 续写、扩写、选区改写也应注入轻量分卷上下文，避免局部操作偏离当前卷目标。
- 当前章节所在分卷应在写作台顶部可见，帮助用户确认 AI 正在承接哪一卷。
- 分卷总结只作为上下文约束，不直接覆盖设定库；正式设定变化仍走定稿后的待确认流程。
- 写作台必须提供 AI 上下文预览入口，让用户在生成前看到本次会注入的创作种子、圣经、本章目标、分卷上下文、设定库、最近设定变更、纠偏任务、Canon 事实、伏笔和顺序规则。
- 上下文预览只用于透明化和排查，不直接改变 Prompt；用户如发现上下文缺失，应回到对应模块补充或确认资料。
- 上下文预览应支持复制全部上下文或单个模块上下文，便于排查模型输出问题。
- 上下文预览中的缺失项应提供跳转入口，能回到项目页对应模块补齐资料。

## 6.10 写作台

写作台是主界面。

布局：

- 左侧：项目章节、版本、候选。
- 中间：正文编辑区。
- 右侧：本章记忆、角色状态、伏笔、AI 操作。

编辑器：

- MVP 使用纯 textarea。
- 支持自动保存。
- 支持选区改写。
- 支持选区“去 AI 腔/润色”：在不改变剧情事实、人物意图和信息量的前提下，降低套路化反差句、虚化判断和解释腔，修正语病并改善段落节奏。
- 支持版本保存。
- 支持候选版本对比。
- 支持对比池差异视图：从对比池中选择基准版本和对比版本，查看段落级新增、删除、改动摘要，再决定是否加载对比版本到编辑器。

AI 操作：

- 生成本章。
- 继续写。
- 扩写场景。
- 压缩场景。
- 改对白。
- 加强冲突。
- 加强人物心理。
- 改成更有网感。
- 改成更文学化。
- 生成多个候选版本。
- 检查逻辑。
- 检查人物是否跑偏。
- 总结本章。

生成本章质量护栏：

- 生成提示词前置轻量审稿规则，但只作为创作边界，不写成固定模板。
- 允许模型在小纲基础上补充场景、对白、细节和合理意外，不压制创意。
- 候选版本阶段不自动改章节标题，避免不同候选版本来回切换导致章名漂移。
- 用户定稿时，如果当前章节标题仍是默认“第 N 章”，系统自动基于定稿正文生成一个默认章名并写入章节元数据。
- 章名必须像目录标题，而不是正文片段或剧情摘要；系统应过滤“主角名 + 在/被/进入/发现 + 动作地点”的流水句式，失败时可二次生成，仍失败则不阻断定稿。
- 默认章名生成失败时不阻断定稿；后续如新增章节标题编辑入口，应只调整章节元数据，不回改已定稿正文。
- 写字台顶部、左侧章节列表和项目章节列表应同时保留章节号和章名；写字台顶部可将章名单独显示在“第 N 章 / 第 N 卷 / 已有小纲”下方，避免截断。导出正文标题使用“第 N 章 · 章名”，没有章名时使用“第 N 章”。
- 章节正文生成不得输出 `# 第N章`、`第N章`、`第十二章 章名` 等标题行；正文清洗只移除开头标题/说明，不应误删正文内部正常叙事句。
- 已确认设定、角色状态、上一章结尾和本章小纲核心节点不得被无解释推翻。
- 允许隐藏真相揭示和认知反转，但必须通过角色认知有限、伏笔回收或误导解除来成立。
- 输出前要求模型静默自检并自行修正承接、小纲完成、设定冲突、新增关键设定和开头乱序问题；自检过程不得输出到正文。
- 生成正文前置“连续性防线”：时间线连续性、状态延续、道具来源、人物铺垫和伏笔铺垫必须先在模型内部检查；如果缺口明显，应在正文中补足动作、证据、交接或心理动机，不把问题留给审稿阶段。
- 生成正文时降低 AI 腔，非对白叙述中“不是X，而是Y / 不是X，是Y”整章最多 2 次；需要表达反差时优先使用具体动作、感官、物象、对白停顿和人物反应。
- AI 腔判断不只看句式，还要看三层痕迹：语言痕迹（高频反差句、短句独段、段首重复主角名、模板结尾）、叙事功能过满（每段都推进/解释/钩子、没有呼吸闲笔）、人性体验缺失（感官打勾、无效数字、情绪贴标签、失去过程跳过）。
- 生成正文时保持正常小说句式节奏：长中短句混合，普通叙事段落通常由 2-5 句组成，用中长句承载动作因果、感官连续、人物观察和心理余波。
- 生成正文时避免高频“主角名 + 动作”段首：同一角色姓名不应连续多段作为段首主语反复点名；除非需要消除多人场景歧义，否则应交替使用动作、物象、环境、感官、对白、心理余波、代词或省略主语来起段，降低机械重置视角的 AI 痕迹。
- 短句独段只作为局部节奏工具，用于战斗、惊悚、濒死、情绪断裂或章节钩子；不要把整章写成“一句一段”的分镜脚本。短句限制是质量提示，不应为了避开短句而牺牲情绪节奏。
- 生成正文时前置“人物代入感 / 人性动机”护栏：关键人物必须有欲望、恐惧、遮掩、选择、代价和情绪残留；外部事件必须通过人物内在动机产生代入感，而不是只靠设定、爽点或华丽句子推进。
- 生成正文时应保留少量“非功能性但有人味”的细节，例如下意识动作、口头习惯、局促沉默、生活痕迹、没有立刻说出口的反应；这类细节用于提升真人感，不能全部被节奏或字数压掉。
- 数字、术语和五感描写必须服务当前场景：数字或专业术语应影响风险、选择、代价、误判或剧情后果；感官描写应选择当前视角最敏感的一两种写深，不做视听嗅触味平均打勾。
- 重大失去不能一句带过：记忆、情感、存在痕迹、亲人、能力或道具的失去，需要写出习惯落空、迟疑、身体反应、自我欺骗、残留动作或迟来的疼。
- 生成正文时注入最近 3-5 章结尾片段，要求模型避免重复“抬头 / 转身 / 闭眼 / 握拳 / 走进黑暗 / 状态总结 / 内心独白收束”等章节结尾模板。
- 人性变化不能写成开关：失去或获得某种情感时，需要残留、迟疑、误读、自我辩解、反复或身体反应，避免把复杂心理简化成“状态切换”。
- 配角不能只做工具人：本章如有配角出场，至少一个配角应有自己的短期目的、习惯、顾虑、私心或不直接服务主线的细节。
- 重大信息应优先通过证据、行动后果、失败尝试、道具反应或误判解除呈现，减少老人、系统、反派、会议或旁白长段信息倾倒。

小纲质量护栏：

- 小纲生成阶段只做 AI 静默自检，不新增人工审查环节，避免流程过重。
- 小纲输出前应自行检查上一章承接、自然时间顺序、设定一致性、人物行动合理性、本章目标完成度和正文发挥空间。
- 小纲阶段要规划句式节奏，明确哪些地方可以短句密集，哪些地方应保持自然段落，避免从源头诱导正文短句化。
- 小纲允许设计隐藏真相和反转，但必须通过角色认知有限、伏笔回收或误导解除成立，不能把正文带向无解释偏离。
- 小纲需要提前规划“人性渐变与配角自主”“信息揭示方式”和“结尾形态”，把 AI 容易写成模板的风险尽量前置到章前设计阶段。
- 小纲必须包含人物动机层，至少说明关键人物想得到什么、害怕失去什么、为什么不能直接说出口、选择要付出什么代价，以及场景结束后的情绪残留。
- 小纲必须包含“连续性自检”：时间线连续性、状态延续、道具来源、人物铺垫和伏笔铺垫；小纲压缩时也必须保留这些关键提醒。
- 小纲目标 700-1100 字，超过 1300 字时应进入压缩；压缩结果只有进入 1300 字以内才算通过，避免过长小纲把两章内容塞进一章。

## 6.11 定稿与记忆入库

章节定稿前先进入一致性审稿流程，避免正文锁定后才发现可修订问题。

定稿前：

- 点击定稿后先执行本章一致性审稿。
- 如果发现严重/主要问题，暂不定稿，弹出审稿报告。
- 用户可以选择生成局部修订版本、仍然定稿或返回修改。
- 局部修订版本应优先基于审稿 `location` 生成可精确匹配的替换补丁，只替换有问题的片段；无法精确匹配的补丁不自动应用，避免整章重写引入新问题。
- 如果只发现轻微/建议类问题，可以由用户确认后继续定稿。
- AI 不自动修正文稿，也不自动定稿修订版本。

定稿后：

- 锁定正文、小纲和最终版本。
- 执行章节摘要、关键事实、待确认设定变更、人物状态、伏笔状态等记忆入库处理。
- 不再重复执行本章审稿，避免出现“已经定稿后才发现问题”的错位流程。

系统生成“待确认记忆变更”：

- 新增事实。
- 修改事实。
- 角色状态变化。
- 软状态变化。
- 人物归属、境界、功法、武器、位置变化。
- 势力、地点、世界规则变化。
- 人物关系和势力关系变化。
- 状态变更日志。
- 新增伏笔。
- 推进伏笔。
- 回收伏笔。
- 章节摘要。

用户可以：

- 全部确认。
- 逐条确认。
- 合并编辑后确认。
- 忽略。
- 稍后处理。

状态：

- pending_review。
- accepted。
- rejected。
- edited。

## 6.12 版本系统

每章支持多个版本。

版本类型：

- ai_candidate。
- correction_candidate。
- final。
- manual_draft。

版本对比规则：

- 普通候选、多模型候选、纠偏候选都可以加入对比池。
- 对比池差异视图只负责辅助判断，不自动采纳、不自动定稿、不自动覆盖当前编辑器内容。
- 纠偏候选需要和当前定稿或当前草稿对比后再决定是否采纳，避免审稿建议直接改变作者已经确认的正文。
- user_draft。
- polished。
- final。
- archived。

不要让 AI 直接覆盖正式稿。

## 6.13 导入导出

MVP 必须支持：

- 导出全书 TXT。
- 导出 Markdown。
- 导出项目 JSON 备份。
- 导入项目 JSON 备份。

后续支持：

- Word。
- EPUB。
- 分卷导出。
- 章节单独导出。

## 6.14 设定库与世界观一致性系统

设定库是长篇小说的结构化记忆底座，尤其适合玄幻、修真、历史、穿越、宗门、家族、国家战争等信息量大的题材。

它解决的问题不是“写得更花”，而是防止长篇写作中出现人物境界错乱、功法前后矛盾、亲属关系忘记、势力位置混乱、神器归属错误等硬伤。

### 6.14.1 定位

- 创作圣经：作品级总纲，记录核心原则、风格、主题、世界底层规则。
- 设定库：实体级资料库，记录人物、势力、地点、功法、物品、关系和每次状态变化。
- 可能性池：未确认脑洞，只能作为灵感，不能直接进入正式设定。
- 记忆入库：章节定稿后的变更确认流程，是设定库更新入口。

第一版不要求用户开局填写完整百科。更合理的方式是“最小设定启动，边写边更新，定稿后确认入库”。

### 6.14.2 人物档案

人物档案是设定库第一优先级。

字段方向：

- 基础信息：姓名、别名、称号、性别、年龄、身份、首次出场章节、最后出场章节、当前状态。
- 归属信息：家族、宗门、门派、国家、阵营、职位、辈分、公开身份、隐藏身份。
- 修炼信息：当前境界、境界层级、主修功法、辅修功法、战斗风格、突破条件、限制代价。
- 物品信息：武器、法宝、丹药、储物物品、坐骑、关键信物。
- 性格信息：性格标签、欲望、恐惧、执念、误解、秘密、底线。
- 关系信息：亲属、师承、好友、敌人、盟友、情感关系、债务恩怨。
- 当前状态：所在地点、身体状态、心理状态、当前目标、已知信息。

### 6.14.3 势力档案

用于记录家族、宗门、国家、军队、商会、秘密组织等。

字段方向：

- 势力名称、类型、地理位置、控制范围。
- 掌权者、继承体系、等级结构、核心人物。
- 资源、功法传承、禁地、敌对势力、盟友势力。
- 明面目标、隐藏目标、当前冲突。
- 相关章节和状态变化记录。

### 6.14.4 世界观档案

用于记录世界的空间结构和基础规则。

字段方向：

- 国家、州郡、城池、宗门驻地、秘境、战场、禁区。
- 父级地点、相邻地点、距离关系、地貌、气候、资源分布。
- 控制势力、危险等级、出入限制、历史事件。
- 与主线相关的世界规则和例外规则。

### 6.14.5 修炼 / 能力体系

用于记录玄幻、修真、异能、武侠、历史权力体系等“成长规则”。

字段方向：

- 境界体系：境界名称、层级顺序、突破条件、实力边界。
- 功法体系：功法名称、品阶、类型、来源、修炼要求、效果、代价。
- 物品体系：武器、法宝、丹药、阵法、符箓、秘宝的等级和限制。
- 禁忌规则：不能突破的边界、特殊例外、违背规则的代价。

AI 写作时必须优先遵守这些硬规则，不能为了爽点临时改规则。

### 6.14.6 关系图谱

关系不只记录“谁认识谁”，还要记录关系的性质和变化证据。

字段方向：

- 主体、客体、关系类型。
- 关系强度、公开关系、隐藏关系。
- 当前立场：亲近、中立、敌对、利用、误解、欠债、血仇等。
- 证据章节、变化原因、是否已被角色知道。

### 6.14.7 状态变更日志

每次关键变化都要有可追踪记录。

字段方向：

- 实体类型：人物、势力、地点、功法、物品、关系。
- 实体 ID。
- 字段路径。
- 旧值、新值。
- 发生章节。
- 原文证据或摘要证据。
- 状态：待确认、已接受、已拒绝、已编辑。
- AI 置信度。

状态变更日志要支持后续回看，避免只保存“当前状态”导致不知道为什么发生变化。

待确认设定变更必须允许确认前编辑。AI 提取结果不能默认完美，用户应能在入库前修正：

- 实体类型。
- 绑定已有实体。
- 实体名称。
- 变更类型。
- 字段路径。
- 旧值和新值。
- 发生章节。
- 原文证据或人工说明。
- AI 置信度。

编辑后仍保持待确认状态，只有用户点击确认后才会写入正式设定库。

### 6.14.8 写作同步流程

标准流程：

0. 项目初始阶段：从创作圣经和当前选中种子做一次性初始化提取，生成待确认设定候选；确认后形成最小设定底座。
1. 写作前：上下文构建器读取本章相关人物、势力、地点、功法、物品和最近变更。
2. 生成中：AI 按当前设定写作，不自动改写正式设定。
3. 定稿后：AI 只从本章定稿正文中提取候选设定变化，并参考已有设定库避免重复。
4. 待确认：用户逐条确认、编辑、合并或拒绝。
5. 入库后：更新人物档案、势力档案、世界观档案、关系图谱和状态变更日志。
6. 下一章：上下文构建器注入最新状态，避免继续写作时遗忘。

禁止混用：

- 初始化提取不能在已有正文后重复执行。
- 章节增量提取不能拿创作圣经或种子重新生成全量设定。
- 如果后续确需大改世界观，应通过人工设定变更、纠偏任务或新草案机制处理，而不是重新初始化设定库。

设定库清空规则：

- 项目没有正文内容时，可以通过二次确认清空设定库并重新从圣经初始化。
- 项目已有正文内容后，禁止清空设定库。
- 写作开始后仍允许维护单条设定、关系和待确认变更，但必须保留变更记录。

### 6.14.9 上下文注入原则

设定库不能把所有资料一次性塞给模型。

注入优先级：

- 本章出场人物的当前硬状态。
- 本章相关势力和地点。
- 与本章冲突直接相关的功法、境界、物品。
- 最近 3-5 章发生的关键变化。
- 与本章目标有关的关系和伏笔。

不相关的历史资料只在审稿或检索时使用，避免上下文过长导致模型注意力稀释。

### 6.14.10 第一版不做

- 不做复杂地图编辑器。
- 不做强制全书世界百科填写。
- 不做未经用户确认的自动设定覆盖。
- 不做重型知识图谱可视化。
- 不做向量库依赖，第一版先用结构化表和关键词关联。

## 7. 技术栈

当前技术栈：

- 前端框架：Vue 3 + Vite。
- UI：Naive UI。
- 样式：Tailwind CSS。
- 状态管理：Pinia。
- 路由：Vue Router 4。
- 后端：FastAPI（Python 3.12）+ aiomysql 异步连接池。
- 数据库：MySQL 5.7，utf8mb4 字符集，LONGTEXT 存正文，JSON 存数组/对象字段。
- API：RESTful，CORS 允许 localhost:5173。
- AI 调用：前端 fetch + ReadableStream 直连 AI 供应商。
- 编辑器：纯 textarea。
- 导出：前端调用后端全量导出 API 生成 TXT / Markdown / JSON。

暂不引入：

- 登录系统。
- 云同步。
- AI Gateway（后端 AI 代理）。
- 向量检索。
- 富文本编辑器。

## 8. 目录结构

```txt
Novel_Creater/
├── frontend/                   # 前端 (Vue 3 + Vite)
│   ├── src/
│   │   ├── api/
│   │   │   ├── ai/
│   │   │   │   ├── index.js
│   │   │   │   ├── adapterBase.js
│   │   │   │   ├── anthropicAdapter.js
│   │   │   │   ├── openaiCompatibleAdapter.js
│   │   │   │   └── providerPresets.js
│   │   │   └── db/
│   │   │       └── client.js      # MySQL API 客户端层
│   │   ├── components/
│   │   │   ├── layout/            # Sidebar, TopBar
│   │   │   ├── settings/          # ProviderSettings, ProviderForm, TaskModelBinding
│   │   │   ├── seed/              # SeedWorkbench, SeedCard
│   │   │   ├── bible/             # CreativeBible
│   │   │   └── writer/            # AIActionPanel, ChapterVersionList, CanonReviewPanel, ContextMemoryPanel
│   │   ├── stores/
│   │   │   ├── projectStore.js
│   │   │   ├── providerStore.js
│   │   │   ├── seedStore.js
│   │   │   ├── novelStore.js
│   │   │   ├── writerStore.js
│   │   │   └── memoryStore.js
│   │   ├── prompts/               # 9 个 AI prompt 模块
│   │   ├── utils/
│   │   │   ├── contextBuilder.js
│   │   │   ├── export.js
│   │   │   ├── backup.js
│   │   │   └── id.js
│   │   ├── views/                 # HomeView, ProjectView, WriterView, SettingsView
│   │   ├── router/
│   │   └── main.js
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
├── backend/                      # 后端 (FastAPI + MySQL)
│   ├── main.py                   # FastAPI 入口 + CORS
│   ├── config.py                 # MySQL 连接配置
│   ├── database.py               # aiomysql 异步连接池
│   ├── schema.sql                # 14 张表，utf8mb4
│   ├── requirements.txt
│   └── routers/
│       ├── projects.py           # /api/projects
│       ├── providers.py          # /api/providers
│       ├── chapters.py           # /api/chapters + versions + tempDrafts
│       ├── seeds.py              # /api/seeds
│       ├── novel.py              # /api/bible, outline, characters, plot-threads, canon-facts, possibility-cards
│       └── export.py             # /api/export/full, /api/import/full
├── DEVELOPMENT_LOG.md
└── PRODUCT_DEVELOPMENT_PLAN.md
```

## 9. 数据结构

本版不再把所有数据塞进单条 novelData JSON blob。为了长期维护，核心对象拆表。

### 9.1 projects

```js
{
  id: 'uuid',
  title: '',
  genre: '',
  description: '',
  targetWords: 1000000,
  targetChapters: 500,
  currentChapterNum: 1,
  status: 'drafting',
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### 9.2 providerProfiles

见 6.2.2。

### 9.3 taskModelBindings

见 6.2.3。

### 9.4 marketItems

见 6.3.5。

### 9.5 creativeSeeds

```js
{
  id: 'uuid',
  projectId: 'uuid',
  title: '',
  genre: '',
  logline: '',
  protagonist: '',
  desire: '',
  coreConflict: '',
  worldPressure: '',
  openingHook: '',
  emotionalPromise: '',
  differentiation: '',
  styleTarget: '',
  source: 'user' | 'ai' | 'market',
  status: 'candidate' | 'selected' | 'archived',
  createdAt: timestamp
}
```

### 9.6 possibilityCards

见 6.5。

### 9.7 creativeBible

```js
{
  projectId: 'uuid',
  premise: '',
  targetReader: '',
  styleBible: '',
  themeBible: '',
  worldRules: '',
  writingProfile: {
    primaryStandard: '',
    secondaryFlavor: '',
    customStyleNotes: ''
  },
  forbiddenDirections: [],
  updatedAt: timestamp
}
```

### 9.8 characters

```js
{
  id: 'uuid',
  projectId: 'uuid',
  name: '',
  role: 'protagonist' | 'supporting' | 'antagonist' | 'minor',
  appearance: '',
  personality: '',
  desire: '',
  fear: '',
  misbelief: '',
  secret: '',
  relationshipNotes: '',
  arcStage: '',
  hardState: {
    location: '',
    physicalStatus: '',
    identity: '',
    faction: '',
    items: [],
    knownFacts: [],
    lastUpdatedChapter: 0
  },
  softState: {
    emotion: '',
    currentDesire: '',
    currentFear: '',
    relationshipToProtagonist: '',
    innerConflict: ''
  },
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### 9.9 plotThreads

```js
{
  id: 'uuid',
  projectId: 'uuid',
  title: '',
  content: '',
  status: 'candidate' | 'planted' | 'developing' | 'transformed' | 'resolved' | 'abandoned',
  plantedChapter: null,
  relatedCharacters: [],
  possibleResolveWindow: [null, null],
  resolveOptions: [],
  resolvedChapter: null,
  notes: '',
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### 9.10 rollingOutlines

见 6.9。

### 9.11 chapters

```js
{
  id: 'uuid',
  projectId: 'uuid',
  chapterNum: 1,
  title: '',
  finalVersionId: null,
  status: 'planned' | 'drafting' | 'reviewing' | 'final',
  summary: '',
  wordCount: 0,
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### 9.12 chapterVersions

```js
{
  id: 'uuid',
  projectId: 'uuid',
  chapterId: 'uuid',
  chapterNum: 1,
  title: '',
  content: '',
  versionType: 'ai_candidate' | 'user_draft' | 'polished' | 'final' | 'archived',
  sourceModelId: null,
  promptBrief: '',
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### 9.13 canonFacts

```js
{
  id: 'uuid',
  projectId: 'uuid',
  chapterNum: 1,
  factType: 'world' | 'character' | 'plot' | 'relationship' | 'timeline' | 'style',
  content: '',
  relatedCharacters: [],
  relatedPlotThreads: [],
  evidence: '',
  confidence: 0.8,
  status: 'pending_review' | 'accepted' | 'rejected' | 'edited',
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### 9.14 tempDrafts

```js
{
  id: 'projectId_chapterNum',
  projectId: 'uuid',
  chapterNum: 1,
  content: '',
  savedAt: timestamp
}
```

## 10. Context Builder

上下文构建器是本产品的核心。

它不应该机械塞入所有资料，而是根据任务和 token 预算选择内容。

### 10.1 上下文优先级

正文生成时优先级：

1. 本章目标。
2. 当前草稿或上一版。
3. 近景大纲。
4. 相关角色硬状态。
5. 相关角色软状态。
6. 最近 3-5 章摘要。
7. 当前卷目标。
8. 必须遵守的世界规则。
9. 相关伏笔。
10. 风格圣经。
11. 远景压力。

### 10.2 发散任务上下文

脑洞发散时不应过度约束，只提供：

- 创作种子。
- 风格目标。
- 当前矛盾。
- 不想写的方向。
- 已经确认不能违背的事实。

### 10.3 审稿任务上下文

审稿时提供：

- 当前章节全文。
- 章节摘要。
- 角色状态。
- 伏笔状态。
- 时间线事实。
- 世界规则。
- 人物动机与代入感检查项：人物是否像工具人、选择是否有欲望/恐惧支撑、情绪转折是否有因果、关键选择是否有代价和情绪残留。

### 10.4 Token 预算

保留 tokenBudget 工具：

- 计算上下文长度。
- 按优先级裁剪。
- 对低优先级内容做摘要压缩。
- 为模型输出保留足够 token。

## 11. Prompt 设计方向

Prompt 不追求让 AI 永远服从固定大纲，而是让它理解当前任务的创作自由度。

每个创作 Prompt 都应该包含：

- 哪些事实不可违背。
- 哪些方向可以大胆发散。
- 这次输出是候选，不是定稿。
- 需要给出不同风味的版本时，不要只换措辞，要换情节选择。

核心 Prompt：

- 选题分析 Prompt。
- 原创种子生成 Prompt。
- 可能性发散 Prompt。
- 滚动大纲 Prompt。
- 章节候选生成 Prompt。
- 多模型融合 Prompt。
- 选区重写 Prompt。
- 风格统一 Prompt。
- 章节摘要 Prompt。
- Canon 事实提取 Prompt。
- 一致性审稿 Prompt。

## 12. 开发优先级

### Phase 1：本地地基

- 初始化 Vite + Vue3 + Naive UI + Tailwind。
- 建立路由与基础布局。
- 实现 FastAPI + MySQL 本地数据层。
- 实现项目库。
- 实现本地导入导出 JSON。
- 实现 Provider 配置。
- 实现 Claude 原生适配器。
- 实现 OpenAI-compatible 通用适配器。
- 实现任务模型映射。

验收标准：

- 用户可以创建项目。
- 用户可以配置至少一个模型。
- 用户可以测试模型连通性。
- 项目可以导出并重新导入。

### Phase 2：AI 创作闭环

- 创作种子工作台。
- 可能性池。
- 创作圣经。
- 滚动大纲基础版。
- 写作台。
- 章节候选生成。
- 多版本保存。
- 选区重写。
- 自动保存 tempDraft。
- 确认定稿。

验收标准：

- 用户可以从一个种子生成设定和第一章。
- 用户可以生成多个章节候选。
- 用户可以选择候选进入草稿。
- 用户可以局部改写。
- 用户可以确认定稿。

### Phase 3：记忆与审稿

- 章节摘要。
- Canon 事实提取。
- 角色硬状态提取。
- 角色软状态提取。
- 伏笔候选提取。
- 待确认变更面板。
- 上下文构建器。
- 一致性检查。

验收标准：

- 写完 10 章后，系统能持续带入已确认事实。
- AI 不明显遗忘主要角色状态。
- 系统能提示明显矛盾。
- 用户可以确认或忽略记忆变更。

### Phase 4：选题雷达

- 并抓取已知可读平台的排行榜页面。
- 并发请求 5 个排行榜页面（书旗、纵横、潇湘、52书库、小说阅读网）。
- 正则提取书名/作者/分类/简介。
- 作品卡片展示（平台筛选、分类筛选、分类统计标签）。
- AI 选题顾问对话面板。
- AI 提炼热门题材趋势和卖点。
- AI 结合市场数据和用户想法生成原创创作种子。
- 种子自动存入 creative_seeds 表。

验收标准：

- 用户可以输入关键词，系统自动抓取热门小说榜单。
- 系统按平台和分类展示作品卡片。
- 用户可以与 AI 对话讨论选题，AI 能结合市场数据给出建议。
- AI 能生成创作种子并保存到"创作种子"标签页。
- 输出不复刻具体作品。

### Phase 5：体验增强

- 模型试写对比。
- 多模型融合。
- 风格圣经强化。
- 章节节奏分析。
- 人物弧光视图。
- 伏笔视图。
- Markdown/TXT 导出优化。
- 本地备份提醒。

## 13. 旧方案调整说明

以下旧设计保留，但降级或改造：

- 三级大纲：改为滚动大纲，不强制全书完整规划。
- 角色状态 Diff：保留，但加入软状态和待确认流程。
- 伏笔 URGENT：改为温和提醒，不做硬性截止。
- 一致性预检：保留为手动或定稿后审稿，不默认打断写作。
- AI 指令模式全量替换：改为候选版本，不直接覆盖当前正文。
- ContextPreview：保留为高级调试面板。
- JSON blob novelData：改为拆表。

以下旧设计暂不优先：

- 每章强制弹出大量确认。
- 每次生成都要求完整三层大纲注入。
- 过度复杂的开发阶段一次性铺开。
- 一开始就做全平台自动爬虫。

## 14. MVP 成功标准

第一阶段产品是否成立，不看功能数量，看这个闭环：

1. 用户输入一个创作种子。
2. AI 发散出有想象力的故事方向。
3. 用户选择方向并生成第一章。
4. 用户定稿后系统生成摘要和记忆。
5. AI 写第二章时能接住第一章的事实和风格。
6. 连续写到第 10 章，系统仍能维持主要设定、人物状态和情节连续性。
7. 用户感觉自己是在导演一部不断生长的小说，而不是在填写项目管理表。

## 15. 给 Claude Code 的启动指令

请阅读本文件，按 Phase 1 开始开发本地版。

第一步只做本地地基：

1. 初始化 Vite + Vue3 + Naive UI + Tailwind 项目。
2. 建立路由：HomeView、SettingsView、ProjectView、WriterView。
3. 建立基础布局：Sidebar、TopBar。
4. 实现 FastAPI + MySQL 数据库结构。
5. 实现项目库：新建、打开、删除、导出、导入。
6. 实现 Provider 配置：新增、编辑、删除、测试连接。
7. 实现 Claude 原生适配器和 OpenAI-compatible 通用适配器。
8. 实现任务模型映射。

关键约束：

- 当前版本是本地版，不做后端。
- API Key 仅保存在本地，并在界面提示风险。
- 正文编辑器使用纯 textarea。
- AI 生成内容默认进入候选版本，不直接覆盖正式稿。
- 所有正式内容必须有用户确认动作。
- 所有持久化数据走 `src/api/db/client.js` → FastAPI → MySQL。
- 先跑通本地创作闭环，再扩展选题雷达。

## 16. 未来 SaaS 化技术蓝图

当前版本先做本地版，不引入后端。但代码结构需要为 SaaS 化预留边界。

未来 SaaS 版优先采用 FastAPI 技术栈，因为已有服务端体系可复用，便于统一部署、鉴权、日志、监控和运维。

### 16.1 SaaS 化触发条件

出现以下需求时，再启动 SaaS 化：

- 多设备同步。
- 用户账号。
- 云端项目库。
- 统一模型代理。
- 团队协作。
- 额度控制。
- 付费订阅。
- 自动选题雷达采集任务。
- 云端备份与恢复。

### 16.2 推荐技术栈

- 前端：Vue 3 + Vite + Naive UI。
- 后端：FastAPI。
- API 规范：REST + SSE 流式输出，必要时补充 WebSocket。
- 数据库：MySQL，优先复用现有服务端数据库体系。
- 向量检索：预留可插拔抽象，早期不强依赖；后续可接独立向量库或云厂商向量检索服务。
- 缓存：Redis。
- 后台任务：Celery / RQ / Dramatiq 任选其一，优先选择和现有服务一致的方案。
- 对象存储：S3 / R2 / OSS / COS。
- 部署：优先复用现有 FastAPI 服务部署方式。

### 16.2.1 MySQL 设计原则

SaaS 版优先使用 MySQL 时，需要提前注意：

- 字符集统一使用 `utf8mb4`，避免中文、特殊符号、长文本保存问题。
- 章节正文、版本正文使用 `LONGTEXT` 或独立内容表，不塞进超大的 JSON 字段。
- 项目、章节、角色、伏笔、版本、记忆事实尽量拆表，方便检索、分页和迁移。
- JSON 字段只用于低频变化的扩展信息，不作为核心查询字段。
- 常用查询字段必须建索引，例如 `user_id`、`project_id`、`chapter_num`、`updated_at`、`status`。
- 全文搜索和向量检索不要在第一版强行做重，先保证按项目、章节、角色、伏笔的结构化检索稳定。
- 后续如需语义记忆检索，通过 repository/service 抽象接入外部向量服务，不影响主业务表。

### 16.3 后端核心服务

SaaS 后端至少拆出以下模块：

- 用户与鉴权服务。
- 项目同步服务。
- 章节与版本服务。
- 创作记忆服务。
- AI Gateway 服务。
- 用量统计与计费服务。
- 选题雷达任务服务。
- 导入导出服务。

### 16.4 AI Gateway

未来所有模型请求都通过 AI Gateway，不由前端直连供应商。

AI Gateway 负责：

- Provider 适配。
- API Key 托管。
- 流式响应转发。
- 请求重试。
- 错误归一。
- 限流。
- 成本统计。
- 用户额度控制。
- 日志脱敏。
- 模型 fallback。

### 16.5 当前架构说明

当前版本已完成 FastAPI + MySQL 架构迁移：

- 前端 Vue 组件不直接操作本地存储或数据库，统一经过 `src/api/db/client.js` → FastAPI → MySQL。
- AI 调用仍走 `api/ai` adapter 浏览器直连供应商，不经过后端代理。
- 项目数据支持 JSON 全量导出和导入（通过后端 `/api/export/full` 和 `/api/import/full`）。
- 数据结构保留 `projectId`、`createdAt`、`updatedAt` 等字段，与 MySQL 表结构一致。
- 数据层细节集中在 Store 中，Vue 页面组件不感知存储实现。

未来 SaaS 化时：

- 后端 FastAPI 可直接扩展为多用户版本。
- AI 调用可迁移到 AI Gateway（后端统一代理）。
- 前端写作体验无需变更。

## 17. 版本路线与开发执行协议

当前规划已经可以进入开发，但必须用版本边界约束范围，避免开发中不断加功能导致核心闭环迟迟跑不通。

开发过程必须同时维护 `DEVELOPMENT_LOG.md`，记录已完成内容、关键决策、当前阻塞、下一步任务和验证结果。每完成一个功能块，都要更新该文件，防止上下文压缩或中断后出现开发断层。

### 17.1 版本路线

#### v0.1 本地地基版

目标：让产品能在本地跑起来，并完成项目、存储、模型配置的基础闭环。

范围：

- 初始化 Vue 3 + Vite + Naive UI + Tailwind。
- 建立基础布局和路由。
- 建立 FastAPI + MySQL 数据库。
- 项目库：新建、打开、删除。
- 项目 JSON 导出与导入。
- Provider 配置：新增、编辑、删除。
- Claude 原生适配器。
- OpenAI-compatible 通用适配器。
- 模型连通性测试。
- 任务模型映射基础版。

不做：

- 正文生成完整体验。
- 选题雷达。
- 角色和伏笔系统。
- SaaS 后端。

验收：

- 本地启动成功。
- 可以创建项目并持久化。
- 刷新页面后项目仍存在。
- 可以配置一个模型并测试连接。
- 可以导出项目 JSON 并重新导入。

#### v0.2 AI 创作闭环版

目标：用户可以从创作种子生成章节候选，并定稿保存。

范围：

- 创作种子工作台。
- 可能性池基础版。
- 创作圣经基础版。
- 写作台。
- 章节候选生成。
- 多版本保存。
- 纯 textarea 编辑。
- 选区重写。
- 自动保存 tempDraft。
- 确认定稿。
- TXT / Markdown 导出。

验收：

- 用户输入一个种子后，可以生成第一章候选。
- 至少支持两个候选版本保存。
- 用户可以手动编辑候选内容。
- 用户可以选区改写。
- 用户可以确认定稿。
- 定稿后可导出文本。

#### v0.3 记忆与审稿版

目标：连续写作时系统能记住事实，减少长篇失控。

范围：

- 章节摘要。
- Canon 事实提取。
- 角色硬状态提取。
- 角色软状态提取。
- 伏笔候选提取。
- 待确认变更面板。
- Context Builder。
- 一致性检查。
- 记忆入库确认流程。

验收：

- 连续写 10 章后，系统能带入前文主要事实。
- 角色位置、状态、目标不明显错乱。
- 系统能发现明显矛盾。
- 用户可以确认、编辑或忽略记忆变更。

#### v0.4 选题雷达版

目标：支持热门题材观察和原创选题孵化。

范围：

- 手动粘贴榜单信息。
- 手动录入作品卡片。
- 公开元数据解析。
- 题材聚类。
- 每类作品卡片展示。
- AI 提炼共同卖点。
- AI 生成原创选题机会。

不做：

- 绕过限制抓取。
- 抓取正文。
- 付费内容采集。
- 重型自动爬虫。

验收：

- 可以导入一批作品元数据。
- 可以按题材聚类。
- 可以生成题材趋势总结。
- 可以生成原创选题建议，且不复刻具体作品。

#### v0.5 体验增强版

目标：提升全程 AI 创作质量和可控性。

范围：

- 多模型试写对比。
- 多模型融合。
- 风格圣经强化。
- 章节节奏分析。
- 人物弧光视图。
- 伏笔视图。
- 本地备份提醒。
- 导出体验优化。

验收：

- 同一章节目标可以由多个模型生成候选。
- 用户可以对比并融合候选。
- 系统可以检查风格偏移和节奏问题。

#### v0.6 设定库与世界观一致性版

目标：补齐长篇小说真正需要的结构化记忆能力，让 AI 在 100 章以上创作中能稳定记住人物、势力、地理、功法、物品和关系变化。

范围：

- 新增设定库一级模块。
- 人物档案扩展：家族、宗门、阵营、境界、功法、武器、性格、亲属、师承、友敌关系。
- 势力档案：家族、宗门、国家、军队、商会、秘密组织。
- 世界观档案：国家、区域、城池、宗门驻地、秘境、地貌、资源分布。
- 修炼 / 能力体系：境界顺序、功法品阶、物品等级、突破规则、禁忌规则。
- 关系图谱：人物关系、势力关系、公开关系、隐藏关系。
- 状态变更日志：记录每次归属、境界、功法、物品、位置、关系变化。
- 定稿后 AI 提取“待确认设定变更”，用户确认后入库。
- 上下文构建器按章节目标注入相关设定，不全量塞入模型。
- 分卷规划作为长篇阶段锚点，为后续分卷审稿、阶段总结和全书审稿提供结构基础。

不做：

- 复杂地图编辑器。
- 重型知识图谱可视化。
- 开局强制填写完整世界百科。
- 未经用户确认自动覆盖正式设定。
- 向量库依赖。

验收：

- 可以创建和维护人物档案，记录归属、境界、功法、武器、性格和关系。
- 可以创建和维护势力、地点、世界规则、修炼体系。
- 章节定稿后可以生成待确认设定变更。
- 用户可以确认、编辑、拒绝设定变更。
- 下一章生成时可以读取最新设定，减少境界、功法、归属、物品和位置错乱。

#### v1.0 本地稳定版（待 v0.6 补强后验收）

目标：本地版形成可持续写作工具。

已完成内容：
- v0.1-v0.5 的稳定整合。
- 全量错误处理加固：6 个 Pinia Store + API 客户端（超时 + JSON 保护）+ 3 个视图 + 备份并发锁。
- 健康检查与断网降级：周期性 ping 后端，离线时 NAlert 提示。
- 全局错误边界（onErrorCaptured）。
- 备份可靠性增强（并发锁防重复触发）。
- 文档同步（DEVELOPMENT_LOG.md + PRODUCT_DEVELOPMENT_PLAN.md）。
- 分卷规划、分卷审稿、分卷阶段总结基础版已完成；写作台已接入分卷阶段上下文；全书审稿仍需继续补强。
- 项目级全局审稿基础版已完成，可从主线、人物、设定、伏笔、节奏和读者承诺六个角度检查项目级风险，并保存最近审稿报告。
- 审稿纠偏任务基础版已完成：分卷审稿和全局审稿可以把问题转成纠偏任务，用户确认、处理或拒绝；未完成任务会进入写作台上下文提醒 AI。
- 纠偏任务上下文过滤已完成：只有待确认、已接受、处理中任务进入 AI 写作上下文；完成、忽略和关闭类任务保留历史但不再干扰生成。
- 纠偏任务应用 v2 低风险部分已完成：纠偏任务可生成待确认 Canon 事实或待确认设定变更；正文类仍不自动覆盖。
- 纠偏任务定位处理已完成：伏笔、圣经、分卷/章节类任务可以跳转到对应模块或写作台章节处理。
- 本章审稿局部修订已完成：未定稿章节可从审稿弹窗直接生成局部修订候选版本，只替换精确匹配的问题片段，并自动打开当前正文与修订候选的差异对比；分卷/全局问题继续进入纠偏任务板。
- 本章审稿局部修订稳定性增强已完成：局部补丁支持常见字段别名和空白差异容错；模型不返回补丁或补丁无法命中时，会重试局部补丁，仍失败则提示用户使用定位原文/替换或手动选区改写，不再生成整章重写兜底版。
- 纠偏任务板章节修订已切换为局部补丁候选：单条正文纠偏和同章综合纠偏都只应用可安全定位的局部替换，候选进入版本列表，不覆盖正文，不再调用整章重写修订。
- 定稿后入库阻断增强已完成：章节摘要、记忆事实和设定变更都属于必需后处理；如果记忆事实提取为空或任一必需步骤失败，会保留定稿后处理失败标记，并阻止后续章节小纲/正文等 AI 生成。
- 本章审稿逐条修订面板已完成：审稿报告关闭后仍在写字台保留问题列表，支持定位原文、查看替换建议、逐条替换或忽略，替换后正文进入未另存状态。
- 单章字数目标约束已完成：写字台会根据项目目标字数和目标章节数推导本章建议字数，把范围写入小纲/正文生成提示词，并在生成候选后提示明显超长或偏短的章节。
- 选题顾问生成创作种子必须完成“AI 回复解析/修复 -> 落库 -> 种子页同步”闭环；结局锚点仍作为长篇收束字段保留。
- 创作流程状态已改为真实数据驱动：种子、圣经、设定库、章节和纠偏任务会显示待完善、待确认、已就绪或已开始；已有正文后禁止清空/删除核心规划资产。
- 待确认设定变更支持确认前编辑，用户可以修正实体、字段、新旧值、证据和置信度后再入库。
- 定稿后设定库自动提取增强已完成：提取上下文包含已有关系，保存前做重复候选过滤，设定库支持批量确认/拒绝待确认变更。
- 设定库冲突检测基础版已完成：待确认变更会提示硬设定覆盖风险，单条确认前二次确认，批量确认跳过高风险项。
- 写作台上下文预览基础版已完成：可在 AI 工具区和上下文区打开，查看生成任务将读取的核心上下文和缺失项；支持复制上下文和从缺失项跳转补资料。
- 章节小纲持久化已完成：每章小纲作为章节级资产保存，刷新或切章后可恢复，生成正文和多候选版本前会保存当前确认小纲。
- 多模型试写对比上下文闭环已完成：对比前必须确认本章小纲，候选生成使用写字台统一组装的完整章节上下文。
- 项目当前章节进度同步已完成：生成候选、生成多候选、生成纠偏候选或确认定稿后，项目 `currentChapterNum` 只向前推进，不因修订旧章节回退。
- 章节定稿后设定增量提取 JSON 解析增强已完成：兼容多种顶层字段和 Markdown 包裹，并可调用 JSON 修复，但不复用圣经初始化兜底逻辑。
- 章节小纲备份迁移闭环已完成：小纲表已进入 schema、全量导入导出和项目删除清理链路。
- 设定库写作上下文相关性排序已完成：优先注入与本章目标、当前分卷、最近设定变更和实体出现章节相关的设定，重要度作为兜底排序。
- 人性动机与代入感护栏已完成：小纲、正文生成和本章审稿都会关注欲望、恐惧、遮掩、选择代价和情绪残留，降低“只有设定/爽点、缺少人性带入”的 AI 写作问题。
- AI 腔句式硬约束已升级：正文生成、静默自检、审稿和去 AI 腔/润色都会把“不是X，是/而是Y”等高频反差句作为可计数问题处理，非对白叙述整章最多 2 次。
- AI 痕迹专项护栏已完成：生成上下文会注入最近章节结尾避免模板化收束；小纲和正文生成前置人性渐变、配角自主、信息揭示方式和结尾形态要求；本章审稿新增结尾模板化、表层情绪、工具人、信息倾倒和套话意象等专项问题类型。
- AI 腔人味标准已升级：本章审稿新增感官打勾、无效数字、情绪贴标签、功能过满和失去跳过 5 类问题；正文生成、小纲和去 AI 腔/润色会前置轻量人味方法，强调非功能但真实细节、感官偏科、有效数字和失去过程。
- 圣经/种子到设定库初始化分类型提取已完成：按人物、势力/组织、世界规则/能力体系、地点/物品、长期关系分批提取，并在前端合并去重后进入待确认候选。
- 多候选版本拆分增强已完成：优先使用固定分隔协议解析，同时兼容 Markdown 标题、版本编号和常见中文版本名，避免多个候选粘连成一个版本。
- 写字台 AI 操作上下文加载保护已完成：章节资料和创作上下文未加载完成前，禁止生成小纲、正文、多候选、对比、续写、扩写和选区改写，避免空上下文进入模型。
- 项目目标规模锁定规则已完成复核：只有已有含正文状态章节、正文/候选版本或临时草稿时才锁定目标字数和目标章节数；空章节和小纲不再锁定。
- 纠偏任务“忽略本次”状态已独立为 `ignored`：新忽略任务不再写入 `rejected`，旧 `rejected` 仍作为关闭状态兼容。
- 纠偏设定候选重复生成保护已完成：同一纠偏任务已生成待确认设定候选后，前端按钮显示已生成，后端创建接口也按候选签名幂等返回已有记录。
- 纠偏设定候选手动闭环提示已完成：任务卡片会显示候选待确认、已确认或已拒绝后的下一步，避免把“生成候选”误解为“已经完成纠偏”。
- 选题顾问空种子入库拦截已完成：方向建议或字段不足的方案不会被误判为创作种子；前端解析、前端创建和后端接口都会拒绝空种子。
- v1.0 接口级完整链路冒烟验收已通过：项目、种子、圣经、设定变更、分卷、章节小纲、正文版本、定稿、内容状态锁定、全局审稿、纠偏忽略和项目清理均可跑通。
- 四层滚动规划基础版已完成：分卷规划页新增规划蓝图面板，可生成/编辑长线蓝图、当前卷规划和未来 3-5 章近景滚动规划；近景规划进入章节上下文，长线蓝图只在分卷层展示和低频滚动。
- 项目删除清理已补齐审稿报告和纠偏任务，避免删除项目后留下孤儿数据。
- 设定变更接口已增强，`oldValue` / `newValue` 支持对象或数组入参并统一 JSON 字符串化存储。

待验收：
- 章节生成链路稳定性验收：持久化小纲、正文生成、版本对比、定稿、设定提取必须形成稳定闭环。
- 设定库与定稿提取体验验收：用真实章节验证人物、势力、地点、功法、物品和关系变化的提取质量。
- 纠偏闭环验收：全局审稿、分卷审稿、纠偏任务、纠偏候选、忽略/完成状态和写作台上下文过滤必须一致。
  - 浏览器端手工验证所有 v1.0 功能，重点观察真实页面交互、弹窗提醒、按钮状态和 AI 错误提示。
- 50-100 章以上长篇项目性能与实际写作流程验证。

已排查出的待修复风险：
- 暂无新的已确认阻塞风险；下一步以浏览器端完整链路验收和长篇项目验证为主。

当前执行原则：
- 继续开发必须围绕“长篇可持续创作”主流程推进，不新增偏炫技功能。
- 优先修复影响写作链路的真实问题：生成失败、顺序错乱、上下文缺失、设定错乱、纠偏状态不一致。
- SaaS 化、多用户、计费、云同步、重型爬虫、向量检索和复杂富文本编辑器暂不进入当前版本。

#### v2.0 SaaS 准备版

目标：在本地版验证产品价值后，启动 FastAPI + MySQL SaaS 化。

范围：

- FastAPI 后端。
- MySQL 主业务库。
- 用户账号。
- 项目云同步。
- AI Gateway。
- 用量统计。
- 云端备份。

注意：v2.0 不在当前本地版开发范围内，只保留架构边界。

### 17.2 开发执行原则

- 每次只推进一个版本内的功能，不跨版本偷跑大模块。
- 每个功能块完成后，必须更新 `DEVELOPMENT_LOG.md`。
- 每个功能块都要有最小可验证结果。
- 组件只负责 UI，复杂业务逻辑放到 store/service/repository。
- AI 调用只走 `api/ai` adapter。
- 数据访问集中在 `src/api/db/client.js`、store 和后端 router/service 层。
- 不在 Vue 页面里散落存储、SQL 或接口拼接细节。
- 正式内容必须有用户确认动作。
- 后端先作为本地数据层服务，未来 SaaS 化主要扩展鉴权、部署、租户隔离和任务队列。

### 17.3 每次开发交接记录格式

每完成一个阶段，在 `DEVELOPMENT_LOG.md` 追加：

```md
## YYYY-MM-DD HH:mm - 阶段名称

### 本次完成
- ...

### 修改文件
- ...

### 验证结果
- ...

### 当前决策
- ...

### 未完成 / 阻塞
- ...

### 下一步
- ...
```

### 17.4 开发前安装规划

v0.1 开始时按以下步骤执行：

1. 初始化前端项目。
2. 安装 UI、路由、状态、存储、样式依赖。
3. 配置 Tailwind。
4. 配置 `@/` 路径别名。
5. 建立基础目录结构。
6. 建立 FastAPI + MySQL 数据库。
7. 建立 Provider adapter 基础接口。
8. 跑通本地开发服务器。
9. 跑通生产构建。

每一步完成后记录到 `DEVELOPMENT_LOG.md`。

## 近期补充：全局审稿范围选择

- 全局审稿支持两种范围：全书审稿、指定章节范围审稿。
- 全书审稿用于阶段性总检查，适合完成一卷、一个大剧情段或准备进入纠偏任务前使用。
- 指定章节范围审稿用于局部检查，适合只审最近几章、某一段剧情或某一卷的章节区间。
- 指定章节范围审稿时，系统只把该范围内的章节摘要、Canon 事实和设定变更作为主要审稿上下文，避免无关章节稀释模型注意力。
- 即使只审指定章节，报告也需要说明该范围问题对后续全书推进、人物弧光、设定一致性和伏笔兑现的潜在影响。

## 近期补充：核心规划锁与任务模型映射

- 任务模型映射是所有 AI 调用的优先选择来源；如果项目设置了任务模型，写作、小纲、审稿、纠偏、摘要、结构化提取、选题和润色都必须按任务类型选择 Provider。
- 如果某个任务没有配置模型，允许回退到第一个可用 Provider，保证本地版低配置时仍能跑通。
- 任务模型映射支持显式清空，保存后刷新不得自动恢复为旧值。
- 创作种子、创作圣经初始化提取和设定库初始化属于写作前核心规划。一旦项目已有正文内容，禁止再次新增、生成、导入、修改、选择、删除或清空创作种子。
- AI 选题顾问不能绕过种子页规则；项目已有正文后，对话中生成的种子只能作为参考，不能写入当前项目种子库。
- 项目已有正文后，禁止从创作圣经再次初始化提取到设定库，避免把已写内容所依赖的设定体系回滚或覆盖。
- 项目已有正文后，禁止删除创作圣经、清空设定库、删除设定实体和删除设定关系。
- 章节定稿后的设定变更确认属于写作中增量维护，不受核心规划锁影响；后续章节应通过这种方式逐步同步人物、势力、地点、功法、物品和关系变化。
- 定稿后摘要、Canon 事实和设定变更提取是进入下一章前的必需后处理。如果必需步骤失败，应阻断下一章生成，避免上下文缺失导致设定错乱。
- 章节定稿后，正文、小纲和版本不可再修改；但系统允许通过专用接口写回章节派生摘要，用于后续上下文和章节列表展示。

## 近期补充：真实长篇流程 QA 与多章验收

- 真实流程 QA 不是只验证接口能跑通，还必须验证长篇写作链路是否能持续控制方向：选题、种子、圣经、设定库、章节小纲、正文、审稿、定稿、记忆事实、设定变更和纠偏任务需要形成闭环。
- 每连续生成一批章节（默认 20 章）后，应执行多章一致性验收，检查人物设定漂移、情节前后矛盾、时间线混乱、世界观规则不统一、伏笔丢失或强回收、重复冗余、语言风格漂移、状态承接缺失和上下章衔接断层。
- 多章验收使用章节摘要、章节开头/结尾片段、设定库和 Canon 事实作为上下文，不直接把整本全文塞进模型，避免成本过高和注意力稀释。
- 章节审稿结构化输出失败不能视为“无问题”：连续解析失败时必须记录质量门禁失败，并提示人工复审或进入纠偏流程。
- 审稿修订候选必须控制字数漂移；如果修订导致章节体量大幅变化，应回退到修订前正文或生成新的候选，避免局部修订变成整章重写。
- 真实流程报告应区分初稿候选字数和最终定稿字数，最终以定稿字数判断长期节奏是否稳定。

## 近期补充：章节字数源头约束与审稿局部修订

- 单章字数按项目目标字数 / 目标章节数生成建议范围，默认目标上下 10% 为推荐范围，上下 20% 为硬边界参考。
- 小纲生成必须按单章体量设计，不得把两个大场景或两章内容塞进同一章；剧情自然超量时，应把支线、余波或解释留到下一章。
- 正文生成提示词应明确把硬上限视为“拆章线”，而不是强行截断线；不能为了字数牺牲当前章必须落地的关键动作、情绪转折和因果交代。
- 真实流程 QA 遇到初稿或定稿字数硬性越界时，只保存候选并停止自动审稿、修订、定稿和后处理，避免明显异常章节污染长篇链路。
- 真实流程 QA 可设置“质量保留容忍区”：当章节略超建议硬上限但未严重失控时，允许保留叙事完整版本并记录警告；如果压缩候选明显过短、过长或破坏章节完整性，应回退到更完整的候选，而不是为了字数接受残稿。
- 真实流程 QA 断点续写前必须检查已定稿章节是否存在硬性字数越界；如果已有异常定稿，应先处理异常章节或重建测试项目，再继续后续章节。
- 真实流程 QA 必须与前端定稿链路保持一致：如果章节标题仍是默认“第 N 章”，应在调用后端定稿接口前生成并写入目录式章名；测试脚本不得绕过章名生成、摘要写回、记忆/设定提取等关键写作链路。
- 本章审稿后的“生成局部修订版本”只允许创建可定位、可替换的局部补丁候选。
- 局部补丁必须拒绝过宽、过度压缩或过度扩写的替换，避免把局部修订变成整章重写或摘要式压缩。
- 如果 AI 没有返回可安全应用的局部补丁，应提示用户使用审稿面板的“定位原文/替换”或手动选区改写，不再自动生成整章兜底修订候选。
- 每 20 章多章一致性验收如果返回 `safeToContinue=false`，应暂停继续生成下一批章节，先进入纠偏任务或人工确认；否则会把角色状态、世界规则和关键物品状态的偏差继续放大。

## 近期补充：写作风格与题材标准库

- 新增本地标准库文档 `WRITING_STYLE_STANDARDS.md`，用于沉淀不同题材和风格的写作方法、读者期待、章节节奏、人物驱动、信息释放方式、章节结尾方式、常见风险和审稿检查项。
- 第一版标准库包含 14 套标准：现实主义群像、历史正剧/庙堂、凡人流/慢热修仙、玄幻热血/史诗升级、仙侠宿命/情绪爆发、轻喜剧/反差网感、悬疑解谜/强钩子、知识体系/理性奇幻、民俗志怪/中式诡异、规则怪谈/无限流/生存博弈、都市异能/灵气复苏/幕后组织、女频成长/古言现言/逆袭、短剧爽文/强冲突反转、经营种田/美食生活/治愈。
- 标准库不是仿写库，也不是训练语料库；不得保存、复刻或拼接受版权保护的小说正文，不要求 AI 模仿某一位在世作者的个人风格。
- 标准库只存抽象方法：题材承诺、章节引擎、人性写法、信息揭示、结尾方式、禁忌和审稿项。
- 产品接入方式：项目在创作圣经中选择 1 套“主写作标准”和 0-1 套“辅助风味”；标准选择保存到圣经的 `writingProfile`，不作为训练语料，也不覆盖项目专属风格基准。
- 本地版尚未正式投入使用，标准库接入不做旧测试项目兼容；后续以 `writingProfile.primaryStandard` 和 `writingProfile.secondaryFlavor` 作为唯一写作策略来源。
- 前端展示方式：项目详情顶部和创作圣经页都应显性展示当前写作策略，避免用户误以为标准只在后台生效；真正影响生成的是进入上下文后的精简规则，不在界面堆满长篇标准原文。
- 章节小纲、正文生成、续写/扩写/选区改写和本章审稿只注入简版规则：适用题材、执行规则和审稿重点，避免标准过重导致创意被锁死。
- 上下文预览需要展示“题材/风格标准”，方便排查模型输出是否确实读取了标准库。
- 项目专属高质量样章仍然保留为“风格基准卡”，与通用题材标准共同使用；通用标准管类型方法，样章基准管当前项目的独特声音。

## 近期补充：句式节奏修订、章节状态账本与局部修订容错

- 正文生成后增加轻量句式节奏检测：当整章出现明显“一句一段”、连续短句独段、平均段落过短或高频 AI 腔反差句时，系统可自动触发一次句式节奏修订。
- 句式节奏检测需要识别“同一角色姓名高频段首点名”的机械主语重置问题；当整章多段连续使用同一姓名开头时，可触发节奏修订，优先调整段首承接方式而不改变视角和剧情事实。
- 句式节奏修订只允许调整段落组织、句式长短和套路表达，不允许新增剧情、人物、设定、道具、伏笔或改变结尾钩子。
- 章节上下文新增“章节状态账本”：从已确认设定、已确认设定变更和 Canon 事实中提取人物伤势、物品归属、能力次数、境界、位置、死亡/失去/恢复等硬状态，作为后续章节生成的高优先级约束。
- 状态账本只记录已经确认或已定稿事实，不把候选、待确认或审稿建议写入硬状态，避免未确认内容污染后续章节。
- 本章审稿后的局部修订补丁应具备更强 JSON 容错：允许解析常见尾逗号、单个完整补丁对象和被截断但仍完整的补丁片段。
- 局部补丁定位可容忍常见标点差异，例如审稿片段把句号写成破折号时，只要正文中唯一命中且替换范围安全，可以应用；若存在多处相似命中，仍必须拒绝自动替换。
- 真实流程 QA 必须同步使用上述节奏检测、状态账本和局部修订容错，以便测试结果接近真实写作链路。
- 定稿后 Canon 事实和设定变更提取需要优先识别硬状态：交易次数、剩余寿命、冷却时间、隐性/显性消耗、物品价值/售价、时间流速、持有物数量、伤势、当前位置和境界等级。
- 硬状态提取必须保留精确数字和单位；如果正文出现“第三次交易”“剩余七十一年寿命”“三天后才能再次交易”“某物价值/售价”“不同世界时间比例”，这些内容应进入记忆或设定库候选，不能被普通剧情摘要挤掉。
- 章节状态账本除人物伤势、物品归属和能力次数外，也需要纳入交易次数、剩余寿命、冷却时间、代价规则、物品价值、售价和时间流速，避免长篇推进时数值和规则漂移。
- 章节生成前的连续性防线需要与状态账本配合：状态账本负责硬状态，提示词负责要求模型在时间线、状态延续、道具来源、人物铺垫和伏笔铺垫上主动补足缺口。
- 正文标题泄漏属于生成清洗层问题：系统应在保存候选前剥离开头 Markdown 标题或章节标题，避免导出正文出现重复“第 N 章”。
## 近期补充：写作生成提示词职责边界

- 正文生成链路只负责三件事：创作边界、写作方法、连续性硬约束。不得把完整本章审稿清单、完整 AI 腔检查清单或全量禁忌逐条塞进正文生成提示词；但可以保留少量直接影响读感的轻量创作护栏，例如反差句次数、人味细节、感官偏科、有效数字和重大失去有过程。
- 小纲生成只规划本章容量、场景节拍、人物选择、信息释放、暂不解决事项和结尾钩子；小纲可以包含人物动机层和连续性自检，但不得规定具体正文句式模板，也不得把“去 AI 腔”“审稿重点”写成逐条打卡指令。
- 写作标准库从“执行规则/审稿重点”改为“作家方法”：只向模型注入章节组织、人物方法、信息释放、语言节奏、结尾倾向和避免项，作为气质和方法参考，不作为逐条打卡的硬约束。
- AI 腔、连续短句独段、模板化章节结尾、段首反复点主角名、过度“不是 X，是 Y”、感官打勾、无效数字、情绪贴标签、功能过满和失去跳过等问题继续保留在生成后审稿、节奏检测、去 AI 腔润色和局部替换流程里处理；生成阶段只做轻量预防，不承担完整审稿职责。
- 纠偏任务进入正文生成上下文时必须限量，只注入与当前章强相关、会影响一致性或方向的高优先级任务；低优先级风格建议留在审稿报告和纠偏任务板，不全量塞入模型上下文。
- 正文提示词应鼓励模型通过行动、误判、代价、关系、物件和环境压力来呈现人物，而不是要求模型机械说明“人性”“情绪”“代入感”。
- 生成质量的判断以后以最终正文读感为准：是否有角色真实动机、是否有生活和关系细节、是否有非模板化结尾、是否能自然承接上下章，而不是以提示词约束数量为准。

## 近期补充：质量优先的生成链路瘦身与待确认记忆门禁

- 创作圣经到设定库初始化提取的保守兜底必须是项目无关的通用提取，不得再内置任何测试项目、旧项目或特定题材的人名、势力名、世界观名词。
- AI 返回设定候选失败时，兜底逻辑只能根据当前种子、圣经、主角名、组织后缀、世界规则和能力体系文本抽取候选，不能凭空补入历史测试故事设定。
- 章节定稿后的 Canon 事实属于后续章节写作的动态记忆。只要存在 `pending_review` 的记忆事实，下一章小纲、正文、多候选、续写、扩写和选区改写都应被阻止，直到用户确认或拒绝。
- 待确认设定变更和待确认记忆事实都属于写作门禁。前者保护世界规则和人物关系，后者保护伤势、位置、物品、次数、代价、时间线等动态状态。
- 正文生成上下文只注入“创作边界摘要、写作气质、关键设定边界、章节状态账本、上一章结尾、近景规划和当前小纲”。完整圣经、完整禁忌清单、全量纠偏任务和审稿规则不再直接塞入正文生成。
- 写作标准库进入正文生成时应转换为短的“写作气质/方法提示”，而不是长篇执行清单。审稿规则继续留在审稿、节奏检测、去 AI 腔润色和局部替换链路中。
- 小纲生成应按单章体量规划 4-6 个节拍、700-1100 字，把后续冲突或余波留到下一章。小纲负责容量和因果，不负责规定具体句式。
## 近期补充：真实流程 QA 与生产写作链路一致性

- 真实流程 QA 的目标不是验证“脚本能生成文本”，而是验证用户在写字台真实使用的长篇创作链路是否能支撑百万字级别持续写作。
- QA 脚本不得自建独立的章节上下文、小纲 Prompt 或正文 Prompt；小纲生成必须复用 `buildChapterBeatSystemPrompt` / `buildChapterBeatPrompt`，正文生成必须复用 `buildChapterSystemPrompt` / `buildChapterPrompt`。
- QA 续写上下文必须通过 `buildWritingContext` 组装，按生产链路读取创作边界、设定库、状态账本、Canon 事实、近景规划、上一章结尾和纠偏任务。
- QA 脚本不得把完整种子 JSON、完整圣经 JSON、完整禁止清单或全量纠偏任务直接塞入正文生成上下文；这些内容只能通过生产上下文构造器压缩成与当前章节相关的创作边界和关键提醒。
- QA 辅助审稿、扩写、压缩、节奏修订可以保留测试专用流程，但输入上下文必须来自生产 `writingContext` 的摘要，不能再拼接一套与产品不一致的提示词。
- 后续所有 3-5 章、20 章和百万字级别验收，都以“生产写字台链路”为唯一标准；如果测试脚本为了自动化需要补充报告字段，只能影响测试报告，不得影响章节正文生成。

## 近期补充：真实模型验收后的质量门禁

- 小纲超过 1300 字时必须压缩；如果两次压缩后仍然超过 1300 字，必须阻断正文生成，提示用户重新生成或手动压缩小纲。不能带着超长小纲继续生成正文，否则容易把两章内容塞进一章，后续再靠压缩补丁会破坏章节质量。
- 短章补足只允许做局部缺口补足：补足缺少的动作、反应、因果、接缝或少量过渡，不允许重写整章，不允许新增完整大场景，不允许把下一章冲突、余波或解释提前塞回当前章。
- 短章补足应有新增内容上限，默认只新增约 700-1400 字，最多不超过 1800 字；如果补足后仍然略短，优先保留自然断点和章节完整性，不为了凑字数机械扩写。
- 身体状态、规则数值和势力存灭属于硬连续性：断臂位置、伤势、毒性、昏迷/苏醒、冷却时间、使用次数、距离、时长、境界、代价、阵法规则、宗门/家族/城市/组织存亡与立场，不能在下一章无铺垫突变。
- 如果硬状态必须变化，小纲和正文都必须先写出发现、验证、代价、误判解除或行动过程；不能直接从“受伤”跳到“恢复”，从“灭亡”跳到“仍然存在”，从“冷却未满”跳到“再次使用”。
- 多章一致性验收返回 `safeToContinue=false` 时，应暂停继续批量生成，先把问题转成纠偏任务、写作约束或人工确认项。继续生成会把角色状态、世界规则和关键物品状态的偏差继续放大。
## 近期补充：AI 痕迹二审进入审稿链路

- 本章审稿仍然负责发现问题；AI 痕迹二审只处理与“AI 腔、人味不足、功能过满、情绪贴标签、感官打勾、无效数字、失去跳过、模板化结尾、段首机械点名”等相关的问题。
- AI 痕迹二审不得吞掉硬连续性问题。设定冲突、时间线错乱、物品/伤势/等级状态错误、世界规则冲突、情节因果断裂等仍按一致性问题进入纠偏或人工确认，不因为文本看起来像“风格问题”而被忽略。
- AI 痕迹二审的职责是判断一审提出的 AI 痕迹是否真实存在，并给出处理策略：`ignore`（误报忽略）、`local_window_revision`（局部滑窗修订）、`paragraph_polish`（段落润色）、`outline_replan`（回到小纲重排）、`full_regenerate`（未定稿时整章重生）。
- 二审失败时不阻断审稿结果，系统保留一审问题；二审成功且判定 `ignore` 时，才从后续纠偏任务和修订候选中移除该风格噪声，避免用户看到大量低价值 AI 腔任务。
- 正文生成阶段仍保持“轻预防”：使用写作指纹、角色处境、人味细节、非功能闲笔、有效数字和失去过程等方法提示；不把完整 AI 痕迹审稿清单塞回正文生成提示词。
- 当前质量链路为：写作指纹 -> 场景型小纲 -> 轻规则正文生成 -> 本章审稿 -> AI 痕迹二审 -> 逐条建议/局部滑窗修订 -> 定稿前闸门 -> 记忆与设定提取。
## 近期补充：本地小说样本与真人写作指纹层

- 用户可以在后续版本上传一本小说，或把多本小说放入本地 `小说txt` 目录，由系统离线分析生成“写作样本卡 / 写作指纹卡”。第一版优先做本地离线分析，前端上传入口后置。
- 写作指纹卡不是仿写卡，也不是训练语料。它只提炼可复用的抽象方法：章节进入方式、章节结尾方式、对话方式、人物方法、群像方法、任务/挑战设计、情绪呈现、信息释放、语言节奏和避免项。
- 指纹卡必须明确“禁止复刻”：不得复刻人物名、地名、势力名、专有设定名、原句、连续表达、标志性比喻和独有段落结构；只允许学习写法结构和叙事方法。
- 多本同题材或相近风格小说可以合并成“写作标准卡”，例如“凡人流修仙写作标准”“考据悬疑奇幻写作标准”“轻松群聊都市奇幻标准”。真正进入正文生成链路的是标准卡的简短方法摘要，而不是小说原文。
- 创作圣经中的 `writingProfile` 仍然是写作策略唯一入口：选择 1 套主写作标准和 0-1 套辅助风味。主写作标准决定章节组织、人物方法和叙事气质；辅助风味只补充局部纹理，不得推翻主标准。
- 创作圣经页需要把当前写作策略展示为可读的“方法卡”，至少展示：章节组织、对话方式、人物方法、群像方法、任务/挑战、情绪呈现、信息释放、语言节奏、结尾倾向和避免项，避免用户只能看到两个标签而无法判断它如何影响写作。
- 正文生成提示词只接收压缩后的写作方法摘要；本章审稿和 AI 痕迹二审可以读取同一套方法卡进行风格一致性判断，但不得要求模型模仿某一本具体小说或某位作者。
- 后续离线分析器的输出应保存为结构化 JSON 或 Markdown 标准卡，便于人工审核、合并和删除；未审核的单书样本卡不应直接进入生产写作链路。

### v1 已实现边界

- 已新增离线样本分析器 `tmp/analyze_writing_samples.mjs`，可读取单个 `.txt` 文件或本地 `小说txt` 目录，输出 `writing-sample-analysis.json` 和 `writing-sample-analysis.md`。
- 当前分析器是本地启发式抽样版本：读取开头、中段、结尾窗口，统计章节数、段落长短、对话占比、短段占比、无效数字/AI 对照句等特征，再生成单书写作指纹卡和合并写作标准候选。
- 当前输出只用于人工审核和后续标准沉淀，不自动写入创作圣经，不自动进入生产写作链路。
- 输出中不得包含原文长段、`rawExcerpt`、`sourceText` 或可复刻连续表达；所有卡片必须标记 `noDirectImitation: true`，并明确禁止复刻人物名、地名、势力名、专有名词、原句、标志性比喻和独有段落结构。
- 下一阶段再做前端上传/审核/合并入口：用户上传或选择本地样本后，先得到“待审核样本卡”，确认后才能合并为项目可选写作标准。

### v1.1 前端审核入口

- 设置页新增“写作样本审核”区块，读取前端内置的本地离线分析报告，展示来源文件数、样本卡数、可审核卡数、题材标签和单书方法摘要。
- 用户可以人工勾选样本卡，合并为 `draft` / `auditRequired: true` / `noDirectImitation: true` 的待审核写作标准候选。
- 当前候选只保存到浏览器本地审核池，不自动加入创作圣经的主写作标准/辅助风味选项；进入正式写作链路前仍需要后续“标准库接入/人工确认”步骤。
- 该入口的目的不是仿写某一本小说，而是帮助用户看到本地样本沉淀出的章节组织、对话方式、人物方法、群像方法、任务/挑战、情绪呈现、信息释放和语言节奏。

### v1.2 待审核候选进入正式标准库

- 写作样本审核区新增“确认加入正式标准库”动作。用户必须先勾选样本卡并合并为待审核候选，再手动确认加入正式标准库。
- 正式标准库存储在本地 `novel_creator_official_writing_standards`，只保存抽象方法摘要、来源样本卡 ID、禁止复刻标记和标准元信息，不保存原文。
- 创作圣经的“主写作标准 / 辅助风味”下拉改为读取“内置标准 + 已确认本地自定义标准”。未确认的 `draft` 候选不会出现在下拉选项中。
- 章节生成和审稿读取自定义标准时，仍走统一的 `writingProfile` 和 `formatWritingStyleStandardsForPrompt` 链路，注入的是压缩后的章节组织、对话方式、人物方法、群像方法、任务/挑战、情绪呈现、信息释放、语言节奏和避免项。

## 近期补充：当前章焦点上下文与源头质量护栏

- 写字台正文生成上下文必须先构建“当前章焦点”：由当前章小纲、近景滚动规划、当前卷规划、分卷阶段上下文和相关线索标签共同决定本章真正涉及的人物、地点、物品、势力、硬状态和伏笔。
- 设定库、章节状态账本、最近设定变更、Canon 事实、伏笔线索和纠偏任务都必须按当前章焦点筛选；禁止把全量设定、全量记忆、全量纠偏任务和远期无关支线直接注入正文生成。
- 核心主角或明确标记为核心的实体可以常驻，但普通高重要度实体不能仅凭“重要”进入当前章上下文，必须与本章小纲、当前卷目标或线索标签存在关系。
- 纠偏任务进入正文生成上下文必须更严格：同章任务可以进入；跨章任务默认不进入；全局任务只有在匹配当前章焦点且属于阻断、严重或主要问题时才进入。轻微风格建议留在审稿报告和任务板，不污染正文生成。
- 章节状态账本只携带当前章相关的硬状态，例如伤势、位置、道具持有、剩余次数、冷却、交易代价、境界、时间和势力立场；不相关的历史状态不再粗暴塞入模型。
- 正文生成源头增加轻量质量护栏：普通叙事段落应有长中短句混合，不能把每个动作拆成短句独段；小纲也不能规划成短句密集的动作清单。
- 小纲和正文都要显式检查时间线连续性、状态延续、规则数值延续、道具来源、人物铺垫、伏笔铺垫和势力连续性；如果状态必须变化，先写出发现、验证、代价、误判解除或行动过程。
- 当前调整属于生产链路基础规则，不是旧版本兼容补丁；后续真实流程 QA 必须以这套焦点上下文和质量护栏为准。
