# 产品开发规划

> 当前有效产品规划。历史长文档和旧测试归档已清理；后续以 `STORY_QUALITY_CHARTER.md`、`CURRENT_PROJECT_STATE.md` 和本文件为准。

## 1. 当前事实来源

所有产品决策必须按以下顺序服从：

1. `STORY_QUALITY_CHARTER.md`
2. `CURRENT_PROJECT_STATE.md`
3. 本文件

如果本文件与 `STORY_QUALITY_CHARTER.md` 冲突，以故事质量总纲为准。

## 2. 产品目标

Novel Creator 通过可控链路帮助用户创作长篇小说：

市场/选题 -> 种子 -> 创作圣经 -> 设定库 -> 分卷规划 -> 故事块滚动规划 -> 当前章小纲 -> 正文草稿 -> 审稿/修订 -> 定稿 -> 记忆/设定提取 -> 块级回看 -> 下一章。

平台需要同时保证：

- 长篇连续性。
- 方向可控。
- 章节可读、好看、有推进。
- 阅读负担低。
- 用户流程清晰。
- AI 输出失败时可恢复。

所有模块最终服务于：**低理解成本地讲一个吸引人的故事**。不是服务于堆高级词、堆隐喻、堆审稿指标。

### 模型配置

- 新建项目应继承最近一次保存过的任务模型映射；如果没有历史映射，则保持未配置状态。
- 生成任务选择模型时，必须优先使用当前项目任务模型映射。
- 当前项目没有任务模型映射时，不得静默使用 Provider 列表第一个模型；必须在 UI 或报告中明确提示未配置，只有显式兜底路径可以记录实际使用的 provider/model。
- 长篇 live 报告和阻断诊断应记录关键任务实际使用的 provider/model，避免模型调用不可见。

## 3. 当前核心流程

### 选题和种子

- 选题雷达和 AI 选题顾问帮助用户生成候选种子。
- 只有在没有已选种子时，第一条有效种子才可以自动选中。
- 项目已有真实章节内容后，种子级破坏性操作应被阻断，或进入明确的产品确认流程。
- 种子应保留必要字段，包括适用时的 `endingAnchor`。

### 创作圣经

- 创作圣经是作品级创作契约。
- 它定义前提、读者承诺、风格方向、主矛盾、主要弧线、禁止方向和结局锚点。
- 它不是实体数据库。
- 已进入真实章节创作后，重新生成或覆盖创作圣经应受到限制。

### 设定库和记忆

- 设定库存储相对稳定的实体：人物、地点、势力、规则、物品、能力。
- 记忆和 canon facts 存储动态章节事实：状态、位置、伤势、归属、选择、线索、近期事件。
- 待确认设定变更必须经用户确认后才进入活跃上下文。
- 被拒绝的设定变更不能静默重新成为已接受事实。

### 分卷规划

- 分卷规划应给每卷明确方向：目标、冲突、关键人物、线索、未解决项和交接点。
- 进入章节上下文时，只注入当前卷摘要、上一卷遗留和下一卷粗方向。
- 空分卷骨架只是兜底，不是高质量默认入口。

### 故事块滚动规划

- 故事块是分卷和章节之间的正式规划层。
- 故事块定义一段连续剧情的目标、故事功能、主要压力、人物变化、未解决项和可能容量。
- 故事块不固定章节数，章节数量由实际故事阶段自然决定。
- 每章定稿后执行块级回看，判断继续当前块、更新未执行阶段、拆分未定稿内容、结束当前块或开启新块。
- 故事块启动后只允许向前滚动：已有定稿章节依赖的 `goal`、`entry_state`、已完成阶段和故事任务不能回改。
- v1 不提供旧的回改当前块动作。只允许使用 `adjust_remaining_stages` 调整尚未执行、未被小纲引用、未被定稿章节依赖的剩余阶段；如果后续方向变化较大，应提前结束当前块并开启新故事块承接。
- 当前章小纲必须从当前故事块滚动生成，而不是直接从远景蓝图跳到单章。
- 如果一个小纲或正文容量过大，优先通过 `split_unfinalized_content` 拆分当前章未定稿内容，或把未写内容顺延到后续章节；不得因为超量回改已执行故事块目标。
- 每章不强制强钩子，只需要自然停顿点。

### 章节小纲

- 小纲是正文质量的直接上游。
- 小纲必须先描述具体故事事件。
- 小纲应足够短，便于执行；也要足够完整，避免正文陷入空泛概念循环。
- 小纲不应变成机械检查表。

### 正文草稿

- 正文生成应以故事优先，保持轻量。
- 默认使用清楚、可读的大白话表达。
- 正文应完成本章事件，不要过度扩写，也不要把多章内容塞进一章。
- 正文生成不应携带完整审稿 rubric。

### 审稿、修订和定稿

- 审稿负责发现问题，但不能自行改写产品方向。
- 修订只应在保留事实、事件顺序、选择和结尾的前提下修复局部问题。
- 定稿前必须阻断结构性坏稿。
- 一旦定稿，章节正文锁定；后续不一致优先通过软过渡处理，除非产品层明确批准其他流程。

### 定稿后提取

- 定稿触发记忆、canon 和设定变更提取。
- 提取失败应可重试，不应迫使用户重新定稿。
- 重复提取必须有保护。
- 下一章生成应按当前流程规则等待必要的待处理记忆/设定事项。

## 4. 质量策略

产品质量优先级如下：

1. 读者愿意继续看。
2. 故事清楚。
3. 长篇连续性稳定。
4. 人物有选择和后果。
5. 阅读负担低。
6. 文字自然。
7. 降低 AI 风格痕迹。

降低 AI 风格痕迹不能压过故事清楚度。

生成阶段使用正向引导。审稿和 QA 可以使用更详细的 rubric。硬门禁只用于结构性失败。

## 5. 当前质量边界

硬阻断候选：

- 大段重复。
- 结尾截断。
- 缺少事件闭环。
- 修订稿明显比原稿更差。
- 与已锁定事实矛盾。
- 数据写入异常。
- 定稿后提取流程死锁。

警告或审稿建议：

- 重复 `notXButY`。
- 短句节奏问题。
- 同主语段首问题。
- 可用但稍别扭的章名。
- 轻微字数漂移。
- 轻微 AI 风格信号。
- 可以人工润色的普通表达。

## 6. 当前开发优先级

优先级 1：文档事实来源清理。

优先级 2：故事块驱动生成链路重构。

优先级 3：结构性坏稿的定稿安全。

优先级 4：正文生成提示词降载，并重写为故事优先。

优先级 5：阅读负担审稿维度。

优先级 6：每次结构性调整后，只做小范围真实流程验证。

当前 3-5 章窗口结构健康之前，不扩大成长跑测试。

## 7. 线程协作规则

主产品线程负责：

- 故事质量方向。
- 规则准入。
- 架构边界判断。
- 判断某个局部测试问题是否能升级为全局规则。
- 判断失败应通过补丁、局部重构还是更大范围重构处理。

开发/测试线程负责：

- 执行明确范围的实现。
- 契约测试。
- 真实流程测试。
- 基于证据汇报结果。

开发/测试线程不得：

- 从单个失败样本新增全局写作规则。
- 把软警告升级为硬门禁。
- 把完整审稿清单注入正文生成提示词。
- 未确认就重写核心流程边界。
- 通过更改报告标签隐藏失败。

## 8. 历史参考

旧长文档、旧真实流程报告和临时补丁记录已清理，不再作为默认事实来源。

如需追溯某个历史决策，优先查看 git 历史，而不是把旧规则重新复制回代码或提示词。任何写作规则、硬门禁、故事块边界或定稿行为的调整，都必须先检查 `STORY_QUALITY_CHARTER.md` 和 `CURRENT_PROJECT_STATE.md`。

## 9. 2026-06-19 当前有效决策：样本库优先级

故事块驱动生成链路稳定跑通 3-5 章后，下一阶段第一优先级提升为“样本库 / 创作经验卡 / 写作标准库产品化”。

原因：平台不能长期依赖 AI 腔规则、句式检查和审稿补丁来提高文章质量。真正能从源头提升真人感和故事吸引力的，是让系统学习优秀小说的创作经验，包括章节进入、场景设计、人物对话、群像调度、任务/关卡设计、爽点与代价、信息释放、节奏曲线和章尾收束方式。

样本库不应继续藏在设置页里作为实验入口。后续产品形态应把“创作经验卡”做成和“设置”“写字台”并列的一级功能板块，形成以下链路：

上传/导入样本文本 -> 生成单书创作经验卡 -> 人工审核 -> 合并写作标准 -> 保存到后端写作标准库 -> 项目创作圣经选择主写作标准/辅助风味 -> 进入故事块、小纲和正文生成上下文。

边界要求：

- 样本文本只能用于抽象创作方法，不得把原文、人物名、地名、专有设定、标志性表达直接进入生成上下文。
- 单书卡应记录“为什么好看”和“故事如何推进”，不只记录语言风格。
- 写作标准库应后端持久化，不能长期依赖浏览器 localStorage 或复制候选 JSON。
- 样本库开发不得插队打断当前故事块主链路收口；等故事块链路小范围验收稳定后立即进入该优先级。
