# 当前项目状态

> 上下文压缩后，或者新开发/测试线程进入项目时，先读这个文件。

## 文档优先级

按以下顺序阅读：

1. `STORY_QUALITY_CHARTER.md`：最高层故事质量原则。
2. `CURRENT_PROJECT_STATE.md`：当前有效状态和下一步优先级。
3. `PRODUCT_DEVELOPMENT_PLAN.md`：当前产品规划。
4. `FUNCTION_TEST_CHECKLIST.md`：当前验收清单。
5. `WRITING_STYLE_STANDARDS.md`：写作风格标准库。

只在追溯历史或排查具体问题时阅读：

- `DEVELOPMENT_LOG.md`
- `tmp/**`
- git 历史

旧归档文档和旧真实流程 QA 报告已清理。不要从旧提交、旧测试脚本或旧日志里恢复写作规则，除非主产品线程重新确认。

## 当前线程分工

当前项目采用双线程协作：

- 主产品线程：负责方向、架构边界、故事质量原则、规则准入和最终优先级。
- 开发/测试线程：负责执行已确认任务、运行契约测试、真实流程测试和输出证据。

开发/测试线程在修改以下内容前，必须回主产品线程确认：

- 正文生成提示词策略。
- 硬门禁策略。
- 小纲结构。
- 定稿行为。
- 记忆、设定库、canon 更新规则。
- 长篇质量策略。
- 产品级文档原则。

## 当前最高质量方向

平台要生成可读、好看、能持续推进的长篇小说。

当前修正方向不是“让文章更文学化”，而是：

- 故事优先。
- 默认大白话、清楚、具体。
- 有具体场景。
- 有人物压力。
- 有行动和后果。
- 有网文式阅读动力。
- 不让读者做阅读理解。
- 不把正文写成规则表。

当前最高方向是：**低理解成本地讲一个吸引人的故事。**

AI 风格信号仍然有用，但主要作为审稿和 QA 观察，不能主导正文生成。

## 当前主要风险

最近真实流程测试说明，反复给质量规则打补丁，可能让正文越来越机械。一些草稿出现过：

- 抽象概念循环。
- 物象和场景结构重复。
- 象征性词语过多。
- 过度围绕规则写作。
- 章名变得标签化或奇怪。
- 结构性坏稿进入 final version。

因此当前方向是：

1. 正文生成保持轻量，回到讲故事。
2. 大多数 AI 风格指标退回审稿和 QA 观察。
3. 硬门禁只保留结构性失败。
4. 保证坏稿不能定稿。
5. 保留低阅读负担和读者信任感。

## 当前已知工作项

优先级 1：故事质量总纲和文档清理。

- 状态：本轮文档清理已建立新的入口文件。
- 目标：防止后续线程把历史补丁当成当前规则。

优先级 2：结构性坏稿的定稿安全。

- 近期证据：最新人工查看章节时，发现已定稿章节存在大段重复和结尾截断。
- 期望修复：定稿前必须阻断重复块、截断、修订退化和缺少事件闭环。

优先级 3：正文生成提示词降载。

- 目标：确保正文生成提示词不携带完整 AI 风格 rubric。
- `notXButY`、短段、同主语段首等表层指标，默认作为审稿和 QA 观察，除非极端到破坏结构。

优先级 4：阅读负担质量维度。

- 目标：识别“读者是否需要做阅读理解”。
- 重点观察空泛比喻、抽象名词链、系统式解释，以及读者很难说清楚到底发生了什么的场景。

优先级 5：故事块驱动生成链路重构。

- 目标：新增故事块滚动规划层，把平台从“单章生成 + 后置修补”改为“故事块 -> 自然拆章 -> 逐章生成 -> 块级回看”。
- 不考虑旧项目和旧数据兼容。
- 前端视觉风格可以保留，但信息架构和写作工作流可以重做。
- 旧真实流程报告已清理，不作为新链路验收依据。
- 关键边界：故事块启动后只能向前滚动。已有定稿章节依赖的目标、入场状态、已完成阶段和故事任务不能回改；只能调整未执行阶段、提前结束当前块或开启新故事块承接。

优先级 6：模型配置继承和规划职责清理。

- 新建项目默认继承最近一次保存过的任务模型映射；没有历史映射时保持未配置。
- 生成链路不得在项目无映射时静默取 Provider 列表第一个模型；显式兜底必须在 UI 或报告中写明实际 provider/model。
- 正式规划结构为“分卷规划 -> 故事块滚动规划 -> 当前章小纲 -> 正文生成 -> 审稿 -> 定稿 -> 块级回看”。
- 长线蓝图只保留卷级方向参考，不能重新成为当前章正文生成的主入口。

## 当前测试策略

当 3-5 章小范围测试已经出现结构失败、硬门禁失败或严重质量回退时，不扩大到 20 章。

小范围测试用于验证：

- 当前正文生成提示词表现。
- 小纲是否可执行。
- 定稿安全是否有效。
- 记忆和设定后处理是否正常。
- 章节连续性是否正常。
- 当前轮是否适合继续。

质量报告必须区分：

- 功能阻断。
- 结构性硬失败。
- 故事质量警告。
- AI 风格观察。
- 历史遗留。
- 当前轮结果。

## 当前清理状态

2026-06-17 已清理旧长文档、旧真实流程测试产物、旧截图和临时日志。

当前只保留：

- 当前产品事实来源文档。
- 当前故事块 dry-run 报告。
- 数据库清理前备份。
- 仍用于契约验证和新链路验收的脚本。

新一轮故事块重构测试应使用干净项目重新验收，不引用旧测试项目和旧报告。

## 2026-06-19 当前优先级补充：样本库 / 创作经验卡

当前故事块主链路仍是正在收口的第一任务。等故事块驱动链路能稳定通过 3-5 章真实流程后，下一阶段第一优先级切换为“样本库 / 创作经验卡 / 写作标准库产品化”。

该模块不应继续作为设置页里的实验功能。目标产品形态是与“设置”“写字台”并列的一级功能板块，用来上传或导入优秀小说样本，生成单书创作经验卡，人工审核后合并为可复用写作标准，并在项目创作圣经中选择使用。

重点不是模仿原文，而是抽象优秀小说的创作经验：章节进入、场景承载冲突、人物对话、群像调度、任务/关卡设计、爽点与代价、信息释放、节奏曲线、章尾收束和长篇阶段推进。

任何后续开发线程不得把样本库仅理解为“风格词条”或“复制 JSON 工具”。它是正文质量源头能力，优先级高于继续叠加零散 AI 腔规则。
